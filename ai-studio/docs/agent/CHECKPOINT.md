# Agent Checkpoint

Updated: 2026-09-22

- Repository Layout: Content Studio now lives in `ai-studio/`; run its build, test, Compose, and documentation commands from that directory. Application behavior is unchanged.

- Current Phase: P1.
- AI Infrastructure Foundation v1: DONE / RUNTIME VALIDATED / FROZEN.
- API Surface: existing ContentProject/job endpoints plus `POST|GET|PUT /api/content-projects/{contentProjectId}/script-review`, `POST /api/content-projects/{contentProjectId}/script-review/approve`, `POST /api/content-projects/{contentProjectId}/storyboard-jobs`, `POST /api/content-projects/{contentProjectId}/storyboard-jobs/{jobId}/assets`, `GET /api/content-projects/{contentProjectId}/assets`, `POST /api/content-projects/{contentProjectId}/storyboard-jobs/{jobId}/narration`, `GET /api/content-projects/{contentProjectId}/narration`, `POST /api/content-projects/{contentProjectId}/storyboard-jobs/{jobId}/subtitle`, `GET /api/content-projects/{contentProjectId}/subtitle`, `POST /api/content-projects/{contentProjectId}/storyboard-jobs/{storyboardJobId}/render-jobs`, and `POST /api/content-projects/{contentProjectId}/render-jobs/{renderJobId}/qa-jobs`.
- Validated Flow: ContentProject -> GenerateIdea durable PostgreSQL Job -> Worker -> local Ollama -> structured `Job.Result` -> HTTP retrieval. The API returned `completed` with all six result fields and no worker/lease internals; PostgreSQL confirmed `Succeeded` with a persisted result.
- Runtime Compatibility: `qwen3.6:27b-q4_K_M` consistently failed JSON syntax validation with `ai_malformed_response`; the earlier coding-model smoke failed identically. `qwen3.8:27b-q4_K_M` completed through the existing retry policy after one timeout and one malformed response, with no application change.
- Phase 0 Local Developer AI: Ollama `0.34.1` is active as the WSL2 system service at `http://127.0.0.1:11434`; `OLLAMA_CONTEXT_LENGTH=49152` is verified. Qwen Code is currently `0.24.0` (the prior checkpoint recorded `0.23.4`) and uses `Qwen Code -> Anthropic-compatible provider -> Ollama`.
- Model Roles: developer coding model is `qwen3.6:27b-coding`; validated Content Studio runtime model is `qwen3.8:27b-q4_K_M`. The development configuration now explicitly requests `qwen3.8:27b-q4_K_M`; environment-variable model overrides remain supported.
- Qwen Routing: developer-productivity only. Use Qwen for bounded, repetitive, localized, low-risk work; use Codex for architecture, security/authorization, concurrency, migrations, durable jobs, destructive operations, ambiguous cross-cutting work, and complex debugging. Human diff review is mandatory before committing Qwen changes; do not build a compatibility platform around Qwen/Ollama.
- Identity Asset Contract v1: DONE. Stable identity is `(AssetReferenceId, IdentityAssetVersion)`; authored `AssetReference.Version` may float only until Approved-only materialization. The canonical provider-safe `PinnedIdentityAsset` is exactly `AssetId + Version`; resolver output retains the materialized authored reference and metadata separately. Registry owns metadata/version/approval; store owns bytes/hash/size and keeps physical mapping Infrastructure-private.
- ComfyUI / FLUX Identity Reference Consumption v1: DONE in code and deterministic fake-HTTP tests; USER visual proof pending. `ImageGenerationRequest` accepts 0 or 1 concrete pin (`>1` rejected). Zero pins use the unchanged trusted T2I workflow. One pin is exact-registry validated (Approved, `reference-image`, `image/png`), read through exact-key `IIdentityAssetStore`, size/hash/signature checked, uploaded to local `/upload/image` as multipart `image` + `type=input` with a safe deterministic Infrastructure filename, and passed only to `LoadImage` in the separate trusted Flux.2 Klein edit graph. Provider never resolves latest. No SceneAsset/DB/API/job/Blender/planning change and no pixel-consistency claim.
- GenerateScript Slice: PRD v0.9.2 classifies idea/script drafts as A1 automatic and orders `Validate GenerateIdea -> GenerateScript -> Manual Script Review`; no Idea Approval gate is required. GenerateScript now accepts a selected structured idea, runs as the existing durable job type, validates a structured script, persists it in `Job.Result`, and exposes it through the existing job query. No schema/migration or durable-job semantic change.
- Manual Script Review/Edit: DONE. A completed GenerateScript `Job.Result` remains immutable AI evidence. `reviewed_scripts.content` is the canonical human-reviewed structured script, uniquely keyed per ContentProject and bound to `SourceJobId`; edits increment `Revision` only when canonical content changes, `Draft -> Approved` is the only transition, approval is idempotent, and approved content cannot be edited.
- Qwen Patch Review: APPROVED and committed as `f24f770`; Qwen `0.24.0` clears its file-read cache after auto-compaction, and future Qwen patches still require human/Codex review.
- Basic Storyboard: DONE. `GenerateStoryboard` reuses the existing durable job type with no schema/migration change. The canonical input is the `Approved` `ReviewedScript` (`reviewed_scripts` row), read directly and never from a historical GenerateScript `Job.Result`. Missing project, missing reviewed script, and draft/unapproved script are rejected at enqueue and in the handler (`content_project_not_found`, `storyboard_script_not_found`, `storyboard_script_not_approved`). Result is an immutable structured `Job.Result` (`title`, ordered `scenes[]` of `heading` + `visual`) exposed through the existing `GET /api/jobs/{jobId}`.
- Basic Asset Collection: DONE. Manual registration associates a local file under an approved `Assets:RootPath` with one storyboard scene, using the completed immutable GenerateStoryboard `Job.Result` as input; no external providers or generation. New additive `scene_assets` table/migration `20260918041620_AddSceneAssets` stores only canonical metadata (project, source job, scene index, type, relative path, byte size, SHA-256 content hash, origin, source/creator/license/retrievedAt, createdAt) and never file bytes. One asset per project scene (unique index); external assets require source and license. Path handling rejects rooted/absolute paths, `.`/`..` segments, Windows drive prefixes, and any path that traverses a symbolic link, and every candidate must resolve inside the configured root. Missing project/job/scene/file, duplicate scene, invalid hash, and missing provenance are rejected with stable codes.
- Basic Narration: DONE. Video #1 keeps narration manual; PRD v0.9.2 defers the TTS model, so no TTS/voice-cloning/STT was added. One `narration_tracks` row per ContentProject (additive migration `20260918042508_AddNarrationTracks`) stores only metadata (source storyboard job, relative path, byte size, SHA-256 content hash, origin, source/creator/license/retrievedAt, createdAt), never audio bytes. Registration is gated on an existing project and a completed GenerateStoryboard `Job.Result`, then reuses the existing `IAssetFileStore` for approved-root path safety and integrity hashing. Duplicate narration, missing/invalid storyboard, missing file, root escape, and missing external provenance are rejected with stable codes.
- Basic FFmpeg Composition: DONE. New durable `JobType.RenderVideo` produces one playable MP4 at deterministic `renders/{contentProjectId}/{storyboardJobId}.mp4` under the approved `Assets:RootPath`; no new table/schema (render evidence lives in `Job.Result`: output path, SHA-256, byte size, probe duration, dimensions, storyboard job, narration track, scene count, narration hash). Enqueue and handler both gate on project, a completed `GenerateStoryboard` result, one asset per scene bound to that storyboard, and that storyboard's narration. Handler re-reads every input through `IAssetFileStore` and rejects any file whose SHA-256 no longer matches its recorded hash. External process execution sits behind the single `IProcessRunner` boundary (`SystemProcessRunner` uses `ProcessStartInfo.ArgumentList`, no shell, bounded by `Rendering:TimeoutSeconds`; `FfmpegVideoRenderer` maps start failure/timeout and validates args). The renderer writes to a temp file, probes the result with `ffprobe` (video + audio stream, duration > 0), then atomically promotes it. Composition is minimal: scale/pad to `Rendering:Width`x`Height`, `fps`, one image/video input per scene, `concat`, `libx264` + `aac`, `+faststart`.
- Basic Subtitles: DONE. The v0.9.2 NOW/Video #1 path includes Subtitle, so an optional operator-provided `.srt` is supported. One `subtitle_tracks` row per ContentProject (additive migration `20260918081522_AddSubtitleTracks`) stores only metadata (source storyboard job, relative path, byte size, SHA-256, origin, source/creator/license/retrievedAt, createdAt), never bytes. Registration mirrors narration: project + completed GenerateStoryboard gate, `.srt`-only, external provenance, duplicate/format/missing-file/root-escape rejection, reusing `IAssetFileStore`. Rendering embeds the matching track as soft subtitles (`-i` input + `-map N:s:0` + `-c:s mov_text`) when its `SourceJobId` matches the render storyboard; a missing or mismatched subtitle is ignored, a tampered one is rejected before render. `Job.Result` records `subtitleTrackId`/`subtitleContentHash` when present.
- Basic Render Timing: equal scene duration = narration duration / scene count (marked with a `ponytail:` comment in `FfmpegCommandPlan`). No per-scene alignment or transition system was added.
- Final Video QA: DONE. New durable `JobType.FinalVideoQa` validates a completed `RenderVideo` `Job.Result` against its promoted MP4 with no new table/schema. The handler re-reads the artifact through `IAssetFileStore` (path safety, non-empty, SHA-256) and rejects a stored hash mismatch, then probes with the shared `IMediaInspector`/`FfprobeMediaInspector` process boundary (the renderer now reuses it instead of duplicating ffprobe parsing). It enforces duration > 0, video stream, audio stream, positive dimensions, and the expected subtitle stream when the render result recorded one. Result is an immutable `FinalVideoQaResult` `Job.Result` (output path, content hash, duration, dimensions, stream presence); failures throw stable `qa_*` `JobExecutionException` codes (`qa_artifact_missing`, `qa_hash_mismatch`, `qa_probe_failed`, `qa_duration_invalid`, `qa_video_stream_missing`, `qa_audio_stream_missing`, `qa_subtitle_stream_missing`, plus gating codes). Deterministic checks only; no AI/quality scoring. Enqueue is `POST /api/content-projects/{contentProjectId}/render-jobs/{renderJobId}/qa-jobs`. `PlaceholderJobHandler` now excludes `RenderVideo`/`FinalVideoQa`, fixing a latent `SingleOrDefault` dispatch overlap.
- Video Quality v1: DONE. The existing `FfmpegCommandPlan`/`FfmpegVideoRenderer` boundary gained deterministic scene motion (`zoompan`; `none`/`slow_zoom_in`/`slow_zoom_out`/`pan_left`/`pan_right`), transitions (`cut` concat, `fade` per-scene, `crossfade` `xfade` with overlap-compensated scene duration), libass-styled burned-in subtitles (`subtitles` + `force_style`), and config-driven background music with `sidechaincompress` ducking and renderer-level per-scene SFX. No schema/migration: existing storyboards get motion/transitions from deterministic render defaults (`RenderingOptions.EnableMotion=true`, `Transition=Crossfade`), and `SceneMediaInput.Motion`/`SoundEffect` override them when supplied. Styled subtitles are burned in with no soft `mov_text` stream because FFmpeg's MP4 muxer forces a lone soft track to `default=1` (double-render risk); `RenderVideoResult.SubtitleBurnedIn` records it and `FinalVideoQa` requires a soft stream only when not burned in, with legacy results defaulting to `false`.
- Video Quality v1.1: DONE. Human review of `quality-v1.mp4` flagged large subtitles, cropping pans, and a poor continuous-tone music bed; the fix stayed inside the existing renderer plus a first Visual Asset Engine (no schema/migration, no new runtime). Subtitles now default FontSize 16 / Outline 1 / MarginV 18 (libass renders SRT on a 288-high canvas, so 18 is a ~45px safe margin); the motion default is centered zoom only and pan is opt-in via `SceneMediaInput.Motion`; crossfade default is 0.35s; music gains `highpass=f=120`, default volume 0.28, duck ratio 4, and narration is gain-staged with `loudnorm=I=-16:TP=-1.5:LRA=11,aresample=48000,asetpts=PTS-STARTPTS` only when audio is mixed (the `asetpts` fixes a loudnorm timestamp offset that truncated `amix duration=first`; `aresample` avoids 192kHz output). Visual Asset Engine: `SceneVisualSvg` (pure Application composer, Hero/Cards/Window/Chat layouts + escaping) and `FfmpegSceneVisualRenderer` (Infrastructure, rasterizes via existing `IProcessRunner` + FFmpeg's built-in `librsvg` decoder); visuals reserve the bottom ~90px for subtitles.
- Video Quality v1.2: transition ghosting fix. Optional `SceneMediaInput.VisualKind` (`SceneVisualKind.Photographic` default, `Graphic` for text-heavy/UI/infographic) feeds a deterministic per-boundary policy in `FfmpegCommandPlan`: when the global transition is `crossfade`, any boundary touching a `Graphic` scene uses `xfade transition=fadeblack` (fade through background) so two text layers never overlay; photographic boundaries keep `fade`. `cut`/`fade` and the 0.35s overlap (hence scene timing/offsets) are unchanged. No computer vision, no new dependency, no schema/migration. Demo `src/AIStudio.Api/assets/renders/vq1-demo/quality-v1.2.mp4` (all 8 composed visuals marked `Graphic`) is preserved locally; `quality-v1.mp4`, `quality-v1.1.mp4`, and canonical Video #1 are untouched.
- Durable Visual Asset Pipeline: DONE. New durable `JobType.GenerateSceneVisuals` (no schema/migration) reuses the existing `SceneAsset` persistence/provenance. `GenerateSceneVisualsWorkflow` gates on a completed GenerateStoryboard result (same shared guard as the handler); `GenerateSceneVisualsJobHandler` maps each `StoryboardScene` through the deterministic `SceneVisualPlanner` into a `SceneVisualPlan` (brief + engine + template + parameters), renders it with the selected engine, writes bytes under the approved root through the extended `IAssetFileStore.WriteAsync` (path-safe, hash-verified, idempotent), and registers one canonical `SceneAsset` per scene (`Origin=Local`, `Source=ai-studio:scene-visual-engine`, `Creator=<engine>`). Scenes that already have a canonical/manual asset are skipped, so generation is idempotent and never overwrites existing assets. `RenderVideoJobHandler` marks generated visuals `SceneVisualKind.Graphic` so the v1.2 transition policy applies, while manually registered assets stay `Photographic`. Enqueue: `POST /api/content-projects/{contentProjectId}/storyboard-jobs/{storyboardJobId}/visual-jobs`.
- Visual Planning: DONE. `SceneVisualPlanner` (pure, deterministic) derives layout (Hero/Cards/Window/Chat), a palette cycle, cards/icons, window command/progress, and an optional animation template from the existing `heading`+`visual`; keyword heuristics only, no model call. `SceneVisualEngineSelector` maps template -> engine (`SvgStill`/`ManimAnimation`). No durable plan table/fields; the plan is derived at generation time.
- AI Image Engine (ComfyUI, optional): DONE. `IImageGenerationProvider` now has two repository-owned graphs: the validated existing T2I graph for zero references and a separate Flux.2 Klein edit graph for one concrete Approved pin. The edit path uses only installed core nodes/models and trusted upload; `GenerateSceneVisuals` still sends zero references and retains existing behavior. Engine remains disabled by default; no table/agent/custom node/graph generation.
- SceneVisualPlanner v2 (SVG regression fix): display text is resolved only from the canonical heading plus explicit quoted on-screen strings and a small generic concept vocabulary; layout classification uses heading intent plus structural cues (a real `ollama <verb>` command, >=2 model options) and command extraction requires a real command. Raw `visual` instruction prose is never truncated into cards/hero/bubbles, and Manim `secondary`/`tertiary` only surface quoted chat dialogue.
- Animation Engine (Manim): DONE. `IManimSceneRenderer`/`ProcessManimSceneRenderer` launch the repository-owned `render_scene.py` + `templates.py` through the existing `IProcessRunner` with structured `ArgumentList` (no shell). Template names are allowlisted and only text/palette/duration data is passed; model/planner output can never execute code. Two deterministic templates: `local_ai_flow` and `chat_flow`. Manim is optional (`Manim:Enabled=false` by default); when disabled the planner stays on `SvgStill`, so a missing runtime degrades safely. Manim Community 0.20.1 installed locally in `ai-studio/.venv` (conda-forge, gitignored); no LaTeX needed (Text/shapes only).
- ThreeD Engine (Blender, optional): DONE. New `IThreeDRenderingProvider` + `ThreeDRenderRequest` boundary and `BlenderThreeDRenderingProvider` render one repository-owned template (`Rendering/Blender/templates/local_ai_laptop.py`) to a PNG sequence via `IProcessRunner` (`blender -b --factory-startup --python ... -- --params ... --frames ...`, `ArgumentList`, no shell), then encode H.264/yuv420p with the shared FFmpeg boundary. `SceneVisualEngine.ThreeD` is selected only for at most one scene matching laptop + local/offline AI keywords, never over an existing Manim template, and only when `Blender:Enabled=true` (default false). Only structured palette/duration/seed data crosses into Python; no caller source, paths, or graphs. Failures map to stable `threed_*` codes. CUDA verified (Blender 5.2.2 LTS, RTX 4060 Ti 16 GB); OptiX deferred/non-blocking. No new table, agent, or migration.
- GPU Resource Gate v1: DONE. `IGpuResourceGate` + async-disposable lease, `GpuResourceGate` singleton (`SemaphoreSlim(1,1)`, `GpuResourceGate:Enabled=true` default). Serializes ComfyUI (submit through poll/download), Blender (CUDA render only, released before CPU FFmpeg), and Ollama (`api/chat`) so only one heavy GPU workload runs at a time in-process. Idempotent release, cancellation-safe waiting, structured logs (no prompt/content). Process-local only: no cross-process/manual-job/external-client coordination, no priorities/fairness/VRAM awareness.
- Animated demo: `src/AIStudio.Api/assets/renders/vq2-demo/animated-explainer-v1.mp4` (1585886 bytes, 24s, 1280x720, h264/aac 48kHz stereo, styled subtitles burned in, SHA-256 `fa53b42f3c94c2537fbe691b29f842d4c587659488a8249e80d5ad4f7460e6b8`), built from the real Video #1 storyboard with `SceneVisualPlanner` + the real `FfmpegSceneVisualRenderer`/`ProcessManimSceneRenderer` + `FfmpegVideoRenderer`: 2 Manim scenes (`LocalAiFlow`, `ChatFlow`) and 6 composed SVG stills. Previous demos and canonical Video #1 are untouched.
- Manim/SVG design consistency: the Manim templates now belong to the same design system — palette values are sent as structured `colors` from the single `SceneVisualSvg.GetColors` source, the font is DejaVu Sans, the header reuses the SVG kicker/headline/accent-underline, chips reuse the accent style, and the bottom ~90px subtitle zone is reserved. `local_ai_flow` and `chat_flow` were reworked for larger objects, layered dark backgrounds, and short labels only (LOCAL/PRIVATE/OFFLINE; HALO/SIAP), with no explanatory prose.
- Narration-aware timing (durable parity): `SceneTiming` is the single source of truth for per-scene durations (text-weighted allocation, 2.0s static / 3.5s animated floors summing to the narration duration). `SceneTiming.AllocateForScenes` and `FfmpegCommandPlan.ResolveContentDurations` are shared by the demo harness, durable `GenerateSceneVisuals` (Manim clip duration) and durable `RenderVideo` (scene content durations). `SubtitleTimeline` derives cue boundaries from the same allocation and reads canonical SRT cue text; `FfmpegVideoRenderer` burns a transient derived SRT and only reads the canonical subtitle asset. `SceneMediaInput.Weight` and `FfmpegCommandPlan` carry it into per-scene durations and cumulative crossfade offsets.
- Animated demo v2: `src/AIStudio.Api/assets/renders/vq2-demo/animated-explainer-v2.mp4` (1551000 bytes, 24s, 1280x720, h264/aac 48kHz stereo, styled subtitles burned in, SHA-256 `b74e37b1ffcce24c46df579ca92f633aa1b91354273f175f742533a68de9bae9`), same Video #1 story and mix, with per-scene durations 3.5 / 3.5 / 2.979 / 3.011 / 2.760 / 3.117 / 2.655 / 2.477s. v1 and all earlier demos are untouched.
- Animated demo v3 (local, gitignored): `src/AIStudio.Api/assets/renders/vq2-demo/animated-explainer-v3.mp4` (1588804 bytes, 24s, 1280x720, h264/aac 48kHz stereo, styled subtitles burned in, SHA-256 `966d60ef97e640e18711929389c6076fc2f0e009d8490f33fefedd910dc36225`). Scenes 0-1 Manim clips are byte-identical to v2; scenes 2-7 use the fixed SceneVisualPlanner v2 structured layouts with no raw instruction text. v2 and earlier demos untouched.
- Validation: Release build 0 warnings/0 errors; `dotnet format --verify-no-changes` and `git diff --check` clean. Focused Rendering 141 tests; full suite 338/338 with `ConnectionStrings__DefaultConnection` (0 failed / 0 skipped). New coverage: planner display-text non-leak + layout mapping, `SceneTiming.AllocateForScenes`, `SubtitleTimeline` (build + canonical cue read), durable Manim duration from narration, and durable subtitle re-timing.
- Video #1 E2E: DONE. One real ContentProject (`01b7f0ef-e749-4e1d-bbad-a93d016b2be2`) executed the existing slice: GenerateIdea (completed after 1 retry) -> GenerateScript (initially failed `ai_timeout` x3 at the 120s client timeout) -> approved ReviewedScript -> GenerateStoryboard (8 scenes) -> 8 scene assets -> narration -> subtitle -> RenderVideo (24s 1280x720 h264/aac/mov_text MP4) -> FinalVideoQa (pass). Product AI stayed local (`qwen3.8:27b-q4_K_M`); no paid/external provider. Config correction: `Ollama:TimeoutSeconds` 120 -> 600 (the model runs near 9 tokens/second, so the script 3000-token budget needs more than 120s; the existing `1..600` validation bound already anticipated this). Artifact preserved at `src/AIStudio.Api/assets/renders/01b7f0efe7494e1dbbada93d016b2be2/1526be4b6d434f54999782cc368a1990.mp4`. Full suite 251/251 pass with the DB connection string set. `FinalVideoQa` result is persisted but not yet deserialized by `GET /api/jobs/{jobId}`.
- Audio Mixing / Mastering v1: DONE (service only). New standalone `IAudioMixer`/`FfmpegAudioMixer` normalizes narration (`loudnorm=-16 LUFS`) and optional background music (`loudnorm=-24 LUFS`), ducks music under speech via `sidechaincompress`, applies short music fade-in/out, limits at -1 dBFS and emits a 48 kHz stereo `pcm_s16le` WAV bounded to the narration length (music looped when shorter, trimmed/faded when longer). Values centralized in `AudioMixingOptions`. Inputs are approved asset references resolved through existing `IAssetFileStore`; output is a relative `.wav` under the approved root; execution reuses `IProcessRunner`/`IMediaInspector` with structured arguments (no shell, no raw filter text, no arbitrary executable paths). CPU-only, never acquires `IGpuResourceGate`. No durable job/endpoint/schema/migration. Current intended production path: VoxCPM2 narration + ACE-Step BGM -> Audio Mixing Service -> 48 kHz stereo mastered mix; ACE-Step remains the current default music-generation candidate and Stable Audio 3 Medium remains optional/alternate. AI provider integration is NOT implemented yet.
- Audio Generation v1: DONE (providers only, disabled by default). New `ISpeechSynthesisProvider` (`VoxCpmSpeechSynthesisProvider`) and `IMusicGenerationProvider` (`AceStepMusicGenerationProvider`) boundaries in `Application/Infrastructure.Rendering.AudioGeneration`. Speech maps a curated Indonesian voice set (Formal/Playful/Energetic/Documentary) to approved control text; music takes a natural-language caption, duration, bpm, instrumental flag and optional seed. Both launch repository-owned Python launchers (`Rendering/VoxCPM2/synthesize.py`, `Rendering/ACE-Step/generate.py`) with trusted config paths plus one constrained JSON request (no caller python/paths/CLI flags/shell), persist through `IAssetFileStore`, verify with `IMediaInspector` (audio, duration, expected sample rate; stereo + duration match for music), and hold the shared `IGpuResourceGate` only across the GPU process (released before CPU-only persist/probe). Stable `speech_*`/`music_*` errors. Config `SpeechSynthesis:VoxCPM2:*` / `MusicGeneration:AceStep:*`; no `/home/tama` paths in source. Real local E2E validated (VoxCPM2 48 kHz narration; ACE-Step 48 kHz stereo 12s; `IAudioMixer` final_mix 48 kHz stereo, no clipping). Now wired into the durable GenerateAudio job (Production Orchestration v2). Current intended stack: VoxCPM2 narration + ACE-Step BGM -> Audio Mixing Service -> 48 kHz stereo mastered audio; Stable Audio 3 Medium remains optional/deferred.
- Production Orchestration v2: DONE (no schema/migration). New durable `JobType.GenerateAudio` (`POST /api/content-projects/{id}/storyboard-jobs/{storyboardJobId}/audio-jobs`) runs three staged steps in one job: narration (VoxCPM2 Formal/id-ID), background music (ACE-Step instrumental, 90 BPM, seed 42, duration derived from narration) and the mastered mix (`IAudioMixer`, 48 kHz stereo). Durable reuse lives in `AudioProductionWorkspace`: deterministic `audio/{projectId}/{storyboardJobId}/{narration,music,master}.wav` plus a small `manifest.json` of per-stage fingerprints; each stage persists immediately, so a retry reuses unchanged valid stages and regenerates only missing/invalidated ones. `RenderVideoResult` gained `audioSource`/`masteredAudioPath`/`masteredAudioContentHash`; `RenderVideoJobHandler`/`RenderVideoWorkflow` prefer the validated master and fall back to the legacy narration track. Providers keep their own GPU leases; orchestration holds none; the mixer stays CPU-only. Real E2E on the canonical project produced a 20.48s 1280x720 h264/aac MP4 from VoxCPM2 narration + ACE-Step BGM with QA pass; a second GenerateAudio reused all three stages in 2s. No new model, table, migration, or endpoint beyond the audio enqueue surface.
- Visual Direction + Multi-Engine Choreography v1: DONE (no schema/migration). New deterministic `SceneVisualDirector` + `SceneVisualRouter`, curated animation primitives/choreography, `SceneVisualPlanner` v3 direction-driven, new `SceneVisualEngine.AnimatedSvg` motion-graphics engine, FLUX motion treatment, and a trusted Manim `process_flow` template. Engine policy: technical flow -> Manim; UI/cards/checklists -> Animated SVG; illustration -> FLUX + motion treatment; spatial/device -> Blender; fallback -> SVG. `SceneAsset.Creator` carries `Engine+per-scene fingerprint` for narrow reuse/invalidation; `GenerateSceneVisuals?force=true` intentionally regenerates while manual assets are preserved otherwise. `RenderVideo?variant=` renders a review file without overwriting the canonical MP4. Real canonical E2E regenerated all 8 scenes across Blender + Manim + AnimatedSvg + FLUX and produced `.../1526be4b6d434f54999782cc368a1990-motion-v1.mp4`; a normal re-run reused all 8 assets. Blender Cycles seed int32-range fix included.
- Narrative Sync + Scene Timing + Visual Polish v1: DONE (no schema/migration). `GenerateAudio` speaks the approved `ReviewedScript` (openingHook + sections + closing) mapped deterministically per scene, generates one VoxCPM2 clip per scene (`narration/scene_{i}.wav`, reused when text/voice/locale unchanged), and assembles them into `narration.wav` at speech-driven offsets. Scene length is `introLead + measured narration + outroHold` with the render crossfade overlap, so audio and video share one timeline; choreography beats and phrase subtitles are derived from the same narration window. Quote marks are normalized (VoxCPM2 emits degenerate audio for quoted spans). FLUX prompts forbid typography; a trusted SVG overlay supplies labels. `RenderVideo?variant=` yields a review artifact. Real canonical E2E produced a 190.51s 1280x720 MP4 with per-scene narration and narrative subtitles; companion runner: `scripts/e2e-narrative-sync.sh`.
- Visual Polish v1: FIXED. The `process_flow` bow-tie/hourglass was a degenerate `RoundedRectangle` progress fill (`width=0.05` < `2*corner_radius=0.34`, self-intersecting) grown with `scale_to_fit_width` (uniform scale, exploding height). The fill is now rebuilt as a valid, constant-height, left-anchored capsule per frame and aligned to the window; `_rounded_rect` clamps corner radii. Real targeted re-renders of the Install/Pull scenes are clean (`assets/renders/visual-polish-v1/`).
- Capability Registry v1: DONE (no schema/migration, no production routing). New application-layer `AIStudio.Application.Capabilities` model (`CapabilityId` validated dotted value object + `CapabilityIds`, `CapabilityDescriptor`, `CapabilityProviderDescriptor`, `CapabilityAvailability`, `CapabilityRequirement`, `CapabilityResolution`, `CapabilityGap`, `ICapabilityRegistry`) with a pure deterministic `CapabilityRegistry`: duplicate/unknown registration fails fast, the preferred enabled provider is chosen by priority then provider-id ordinal, a requirement carries an ordered fallback capability chain, and an unresolved requirement reports an explicit gap that distinguishes `CapabilityNotImplemented` from `ProviderDisabled`. `ProductionCapabilityCatalog` maps the current trusted stack: `speech.narration`→voxcpm2, `music.instrumental`→ace-step, `visual.still`→svg-still, `visual.diagram`→manim, `visual.ui_motion`→animated-svg, `visual.ai_image`→comfyui-flux, `visual.three_d`→blender, `media.compose`→ffmpeg-compose, `subtitle.burned`→ffmpeg-subtitle-burn. Registered as a DI singleton built only from the existing enablement options (`SpeechSynthesis`/`MusicGeneration`/`Manim`/`ComfyUi`/`Blender`); no provider is instantiated, contacted, or required, so capability discovery works with every optional runtime absent. Provider descriptors carry ids/priority/runtime category only — never executable or model paths. `SceneVisualRouter` and every production handler are untouched; the registry exists alongside the pipeline for the future ProductionRecipe/Concept Registry.
- Production Recipe v1: DONE (no schema/migration, no production routing). New application-layer `AIStudio.Application.ProductionRecipes` model: `ProductionRecipeId`/`ProductionRecipeVersion` validated value objects, `ProductionRecipe` (id + version + `DisplayName` + requirements — identity is data, never an enum or a subclass), `ProductionRecipeRequirement` (wraps the capability-registry `CapabilityRequirement`, with required/optional), `ProductionRecipeIssue` + `ProductionRecipeValidator` (rejects invalid id/version, empty requirements, no-required requirements, duplicate primary capability, fallback repeating the primary, duplicate fallbacks; a valid but unimplemented capability is still valid data), `IProductionRecipeRegistry`/`ProductionRecipeRegistry` (in-memory, fail-fast on invalid/duplicate id+version, deterministic id-then-version order, `Get`/`GetLatest`), and `IProductionRecipeResolver`/`ProductionRecipeResolver` (delegates every requirement to `ICapabilityRegistry.Resolve`, so provider/fallback/gap rules are never duplicated; no provider is executed). `ProductionRecipeResolution` exposes recipe id/version, producibility, per-requirement `CapabilityResolution`, used fallbacks, and gaps; `ProductionRecipeStatus` is `FullySupported` / `SupportedWithFallbacks` / `Unsupported`, where an unresolved required capability blocks production and an unresolved optional capability is reported but non-blocking. Two seed recipes (`SeedProductionRecipes`, data only): `tech-explainer` v1 (speech.narration; visual.diagram→visual.ui_motion→visual.still; music.instrumental; media.compose; subtitle.burned) and `motion-comic` v1 (speech.narration; visual.ai_image→visual.still; music.instrumental; media.compose; subtitle.burned). Models round-trip through plain System.Text.Json. Registered as DI singletons; `SceneVisualRouter`, all production handlers, and the capability registry behavior are unchanged.
- Concept Registry v1: DONE (no schema/migration, no LLM, no production routing). New application-layer `AIStudio.Application.Concepts` model: `ConceptId`/`ConceptVersion` validated value objects, `ConceptIdentifier` (validates format/style/tag tokens as plain data — never enums), `ConceptManifest` (WHAT to make: id, version, title, description, audience, format, style, recipe id/optional version, duration, tags), `ConceptValidationIssue` + `ConceptValidator` (rejects invalid id/version, empty title, malformed format/style, invalid recipe id, duration outside 1..86400s, invalid/duplicate tags; never scores or judges quality), `IConceptRegistry`/`ConceptRegistry` (trusted in-memory boundary: validates on register, fail-fast duplicate id+version, deterministic id-then-version order, `Get`/`GetLatest`), and `IConceptResolver`/`ConceptResolver`. The concept references a trusted `ProductionRecipe` and never copies its capability requirements; `ConceptResolver` maps the recipe (exact version, or the latest registered version when omitted) and delegates entirely to `IProductionRecipeResolver`, so provider/fallback/gap rules stay in one place. `ConceptResolutionStatus` is `Ready` / `ReadyWithFallbacks` / `Unsupported`; a missing recipe is reported as its own `ConceptResolutionIssueCodes.RecipeNotFound` issue, distinct from a known recipe whose required capability is unavailable (`recipe_unsupported` + inherited `CapabilityGap`s). Two seed concepts (`SeedConcepts`, data only): `local-ai-tech-explainer` v1 (youtube-longform/clean-tech, recipe `tech-explainer` v1) and `local-ai-motion-comic` v1 (youtube-short/motion-comic, recipe `motion-comic`, latest). Concept JSON uses clean scalar values (`[JsonPropertyName]` + converters for `ConceptId`/`ConceptVersion`/`ProductionRecipeId`/`ProductionRecipeVersion`), so a future Qwen/Creative Director can return a `ConceptManifest` directly. Security: concept data carries no type/assembly names, paths, shell/Python/JS, model URLs, provider workflows, or secrets, and can never register an executable capability. `ProductionRecipeRegistry` gained additive non-throwing `TryGet`/`TryGetLatest` (no behavior change). Registered as DI singletons; `SceneVisualRouter`, all production handlers, and capability/recipe behavior are otherwise unchanged.
- Creative Director v1: DONE (no schema/migration, no LLM call, no production routing). New application-layer `AIStudio.Application.Creative` layer sits ABOVE Concept Registry and answers "how should an approved idea be expressed?". It does NOT replace or duplicate Solution Idea / GenerateIdea: `ApprovedIdea` is a neutral boundary (`IdeaReference`, `Topic`, `Angle`, `Audience`, `Objective`, `HookPremise`, `SourceSummary`, `Language`, `CreativeConstraints`) with `ApprovedIdea.FromGenerateIdeaResult(...)` mapping the existing `GenerateIdeaResult` descriptively (Title→Topic, Angle→Angle, TargetAudience→Audience, Hook→HookPremise, Summary→SourceSummary, SuggestedFormat→preferred-format hint) and no job/infrastructure concerns. `ApprovedIdeaValidator` is structural only (topic/angle/audience/hook required; format/style hints are validated data tokens; duration in `1..86400`). The output is `CreativeDirection` = existing `ConceptManifest` (reused, not forked) + a small `CreativeTreatment` (`storyApproach`, `hookTreatment`, `pacing`, `visualStrategy`, `endingTreatment`, optional `tone`/`transitionStrategy`) — descriptive data only, no script/storyboard/provider prompts; both are validated (`ConceptValidator` reused, `CreativeTreatmentValidator`). `CreativeDirectionParser` accepts only valid JSON, rejects malformed JSON and missing concept/treatment, maps the concept into `ConceptManifest`, and performs no speculative repair. `CreativePlanningContextProvider` projects the trusted registries into a SAFE summary: recipe id/version/display name, required capability ids, `resolvable`, `usesFallbacks`, plus available/unavailable capability ids — never provider class names, paths, scripts, or secrets. `CreativeDirectorPrompt` is a deterministic, token-efficient builder that includes the approved idea, the safe recipe catalog, capability awareness, the exact JSON contract, and creative boundaries ("do not invent a new idea", "never change WHAT the idea is", data-driven format/style, select a known recipe, no code/paths/URLs/provider details, do not claim unavailable capabilities). `CreativeDirector`/`ICreativeDirector` validates the idea, builds the prompt, calls the existing `IAiTextGenerator` boundary once with `AiResponseFormat.JsonObject`, parses/validates the untrusted response, and applies a deterministic guardrail that preserves the approved audience and idea reference (no semantic/AI scoring). No auto-registration, no production start, no Qwen call on this machine; `CreativeDirectionOptions.AllowBeyondCurrentCapabilities` (default true) lets the director propose an ambitious style while resolution reports reality honestly. LLM output is untrusted data and can never introduce types, paths, shell/Python/JS, model URLs, provider workflows, or secrets. Registered as DI singletons/scoped; the production pipeline, `SceneVisualRouter`, Concept/Recipe/Capability behavior are otherwise unchanged.
- Story Director Foundation v1: DONE (no schema/migration, no LLM call, no production routing). New application-layer `AIStudio.Application.Stories` layer sits between Creative Director and Script and answers "how should this creative direction unfold as a clear narrative?". `StoryPlan` is declarative data (`StoryPlanId`/`StoryPlanVersion`, a `sourceConceptId` reference that does not duplicate `ConceptManifest`, a data-driven `NarrativePattern` id/version, `targetDurationSeconds`, ordered `StoryBeat[]`). `StoryBeat` is generic (`id`, `order`, data-driven `role`, `purpose`, data-driven `importance`, `targetDurationSeconds`, identifier-only `characterRefs`/`worldRefs`/`continuityFrom`); roles and new patterns are DATA, never enums or director subclasses, so an `anime-horror-reveal` or `kids-song-loop` pattern registers and directs with no C# change. `INarrativePatternRegistry`/`NarrativePatternRegistry` is a trusted in-memory catalog (validate on register, fail-fast duplicate/invalid id+version, deterministic id-then-version order, `Get`/`GetLatest`; no DB/Redis/hot reload/plugins). `StoryPlanValidator` checks structural integrity deterministically (ids, beat id/order uniqueness, positive durations, duration-sum tolerance, references, continuity self/cycle, required pattern slots). `StoryDirector`/`IStoryDirector` is a deterministic v1 scaffold that maps pattern slots to ordered beats and shares the target duration by slot weight; it has no AI/process dependency and throws stable `story_*` codes. `StoryPlanParser` strictly parses future structured JSON (unknown members rejected) and invokes the validator. Seed patterns: `problem-solution-short`, `explanatory-flow`. Creative Director/Script/Storyboard boundaries and all existing jobs/handlers are untouched.
- Character + World Bible v1: DONE (no schema/migration, no LLM, no production routing, no DB, no asset storage). New engine-neutral application-layer `AIStudio.Application.Bibles` layer gives `StoryPlan`'s `characterRefs`/`worldRefs` authoritative definitions. A `CharacterBible` keeps stable IDENTITY (`CharacterIdentity`: data-driven role/species/agePresentation/bodyStyle/hair/eyes, free-text visualDescription and distinguishingTraits; `PersonalityTraits`; `BaselineVariant`; approved `Variants`; data-driven `Relationships`; engine-neutral `AssetReference`s) separate from scene-level `CharacterState` (`characterRef`, `variant`, emotion/pose/action, `worldRef`, `heldProps`). A `WorldBible` keeps stable identity (`WorldIdentity`: environmentType/visualDescription/spatialTraits, recurring props, free-text continuity rules, optional `WorldLocation`s, asset references) separate from `WorldState` (timeOfDay/weather/lighting/temporaryProps/notes). `CharacterBibleId`/`WorldBibleId`/`AssetReferenceId` are validated value objects (shared `StoryIdentifier` rule); `AssetReference` is id + optional identity-asset version + data-driven purpose/variant — never a path/URL/command/workflow/secret; bytes remain outside the Bible contract. `ICharacterBibleRegistry`/`CharacterBibleRegistry` and `IWorldBibleRegistry`/`WorldBibleRegistry` are trusted in-memory catalogs (validate on register, fail-fast invalid/duplicate id+version, deterministic id-then-version order, `Get`/`GetLatest`/`TryGet`/`TryGetLatest`; no DB/Redis/hot reload/plugins). `CharacterBibleValidator`/`WorldBibleValidator` are deterministic structural validators (identity/traits/variants/relationships/locations/asset references, state token validity, approved-variant enforcement) that never score visual similarity or call a model. `StoryContinuityValidator` additively reports unknown `StoryPlan` character/world references against the registries without changing the Story Director or requiring bibles for existing plans. `BibleParser` strictly parses JSON (unknown members rejected) into validated bibles. v1 seeds no identity data; new human/robot/animal/fantasy characters and any world are DATA, not C# subclasses. Existing jobs/handlers are untouched.
- Story Context Builder v1: DONE (no schema/migration, no LLM, no production routing, no DB). New deterministic, AI-free application-layer `AIStudio.Application.StoryContext` layer projects the trusted narrative inputs into a compact, consumer-neutral `StoryContext` for future Script/Storyboard stages. `StoryContextBuilder` (`IStoryContextBuilder`) consumes the existing `CreativeDirection`, `StoryPlan`, `ICharacterBibleRegistry`, and `IWorldBibleRegistry` (no duplication, no mutation) and returns a `StoryContextBuildResult` (`Context` + deterministic `StoryContextIssue`s). The projection is a compact concept summary (`StoryConceptContext`: id/title/audience/format/style/duration; no recipe/tags/description), the reused `CreativeTreatment`, a `StoryNarrativeContext` (plan id/version, pattern/version, target duration), only the referenced `StoryCharacterContext`s and `StoryWorldContext`s, ordered `StoryBeatContext`s (purpose/order/role/importance/duration/refs/continuity; no dialogue or shot detail), an optional `StoryStateContext` for caller-supplied `CharacterState`/`WorldState` kept separate from identity, and the `BeatScope`. Selection is deterministic relevance filtering: dedup + id-ordinal sort for characters/worlds, order-then-id for beats, included-character-only relationships sorted by target then type, and asset metadata excluded by default (id/purpose/variant only when explicitly opted in; never resolved to storage). Whole-story context includes every beat; a request with `BeatId` returns exactly the selected beat plus its transitive `continuityFrom` closure. Reference validation reuses `StoryContinuityValidator` against a non-mutating, scope-copied plan, so unresolved characters/worlds are reported, not dropped. Bible references resolve to the latest registered version deterministically. Works identically for tech/anime/preschool formats with no genre branch. Existing jobs/handlers are untouched.
- Extractable Domain Modules + Reusable Capability Rules: DONE (documentation + guardrail only; no source behavior, runtime, package, schema, or service-split change). `ai-studio/AGENTS.md` gained a `## Modular and Domain Boundaries` rule block: no cross-module persistence/Infrastructure access, application-owned transport-neutral contracts/events, module-owned workers, domain-specific business models, capability extraction only after a second real workflow proves duplication, and no microservice infrastructure until a module is actually extracted. The canonical PRD gained a clearly labeled post-freeze addendum `docs/product/parts/13_EXTRACTABLE_MODULE_BOUNDARIES.md` (explicitly not source-derived; the frozen source and its SHA-256 are unchanged), indexed from `PRD_INDEX.md` and `SECTION_MAP.md`, covering modular-first/distributed-when-justified, co-located-but-remotely-extractable-by-contract, domain-specific workflows, proven-capability reuse, the illustrative (uncommitted) Content vs Stock Intelligence topology, deferred microservices, and ADR-018 Extractable Module Boundaries + ADR-019 Domain-Specific Workflows and Proven Capability Reuse. Durable entries `D-042`/`D-043` were added to `docs/development/DECISIONS.md`, with a pointer from `docs/development/ARCHITECTURE.md`. Enforcement: new cheap assembly-level `tests/AIStudio.Tests/Architecture/ArchitectureBoundaryTests.cs` (reflection only, no new package) checks Domain↛Application/Infrastructure/Api, Application↛Infrastructure/Api, and Infrastructure↛Api; finer-grained cross-module namespace rules are documented as deferred. No Stock implementation, microservice runtime, broker, database split, or remote client was introduced.
- Creative Director Real-Qwen Validation Harness v1: DONE (local harness + script only; no product/architecture change, no schema/migration, no DB, no media). New `.demo/CreativeDirectorQwenValidation` console harness drives the PRODUCTION `ICreativeDirector` over the real `OllamaTextGenerator` (a recording decorator captures the exact prompt/raw response without a parallel creative pipeline) across 5 representative `ApprovedIdea` cases, reporting JSON validity, schema, idea preservation (deterministic token-overlap heuristic), audience preservation (with model-vs-guardrail visibility), recipe id/status (valid / supported-with-fallback / unsupported / hallucinated), format/style, treatment (printed compactly for human review), implementation-leak scan, output size, latency, and RESULT=PASS/REVIEW/FAIL. One-command `scripts/e2e-creative-director-qwen.sh` sources `.env`, preflights Ollama + the configured model, builds, runs an 11-check self-check, then the cases, and preserves full raw artifacts (approved idea, prompt, raw response, parsed direction, validation JSON, timing) under gitignored `.demo/artifacts/creative-director-qwen-<timestamp>/`. The real multi-case Qwen run is USER-EXECUTED; the coding agent must not run it.
- Creative Director real-Qwen validation: PASSED 5/5 (model `qwen3.8:27b-q4_K_M`); all five cases JSON/schema/idea/audience PASS with no implementation leak. Artifacts: `.demo/artifacts/creative-director-qwen-20260921T125832Z`. Creative Director is validated for this milestone.
- Qwen-backed Story Director v1: DONE (small production capability; no schema/migration, no DB/job/endpoint, no media). The deterministic `StoryDirector` is preserved and remains the DEFAULT (`StoryDirector:Mode=deterministic`); new `QwenStoryDirector` implements the existing `IStoryDirector` by building a deterministic prompt (`QwenStoryDirectorPrompt`) from the `CreativeDirection` + caller-supplied `NarrativePattern`, calling the existing `IAiTextGenerator`/`OllamaTextGenerator` boundary, then parsing/validating through the existing `StoryPlanParser` and `StoryPlanValidator` (never bypassed). DI selects `IStoryDirector` from `StoryDirectorOptions` via `StoryDirector__Mode`: `deterministic` → `StoryDirector`, `qwen` → `QwenStoryDirector`; an unknown mode fails clearly and there is no silent fallback (a Qwen failure surfaces). Qwen never selects the pattern; the supplied pattern stays authoritative and the director pins `id`/`version`/`sourceConceptId`/`narrativePattern`/`targetDurationSeconds`. Real-Qwen Story Director validation NOT executed yet; next milestone is the Story Director real-Qwen validation harness.
- Real-Qwen Story Director Validation Harness v1: DONE (local harness + script only; no product behavior change, no schema/migration, no DB/job/API, no media). New `.demo/StoryDirectorQwenValidation` console harness resolves the PRODUCTION `IStoryDirector` through `StoryDirector:Mode=qwen` → `QwenStoryDirector` (real DI selection) with a recording decorator over the real `OllamaTextGenerator`, feeds the five existing real CreativeDirection artifacts from `.demo/artifacts/creative-director-qwen-20260921T125832Z` (Creative Director is NOT rerun), selects the closest EXISTING registered narrative pattern per case (`explanatory-flow` for tech-explainer; `problem-solution-short` for the other four), and reports JSON/schema/concept/pattern/beats/duration/continuity/refs/script/storyboard/implementation-leak/output/latency/narrative/RESULT with per-beat summaries; artifacts are preserved under gitignored `.demo/artifacts/story-director-qwen-<timestamp>/`. One-command `scripts/e2e-story-director-qwen.sh` sets `StoryDirector__Mode=qwen` for its process only, preflights Ollama + configured model, verifies the five inputs exist, builds, runs an 11-check self-check, then the five cases. The real five-case Qwen run is USER-EXECUTED; the coding agent must not run it.
- Story Director Boundary Refinement v1: DONE (prompt-only; no parser/validator/StoryPlan/NarrativePattern change, harness heuristic not weakened). Real-Qwen evidence: case 2 (anime) hit storyboard REVIEW on `camera`/`cut to`, case 3 (preschool) on `close-up`; the words were carried in from the upstream CreativeDirection treatment fields (`endingTreatment` "camera slowly zooms in", `transitionStrategy` "Hard cuts", `hookTreatment` "close-up ... block") and the prompt did not require translating visual context into narrative events. `QwenStoryDirectorPrompt` now labels the creative-direction fields as creative-intent CONTEXT, requires translating them into narrative events/state/purpose, forbids named cinematography/editing execution terms (close-up, medium/wide shot, camera, camera angle, lens/mm, pan, tilt, dolly, rack focus, cut to, hard cut, cross-dissolve, zoom, frame number, shot number, camera coordinates), and explicitly allows narrative visual state (room becomes dark, character notices an object, product appears). Future catalog consideration (NOT this milestone): `problem-solution-short` is not ideal for anime mystery/reveal; consider registering a `mystery-reveal`-style pattern when anime production needs it.
- Real-Qwen validation: Creative Director PASS 5/5 and Story Director PASS 5/5 (after the boundary refinement); artifacts `.demo/artifacts/creative-director-qwen-20260921T125832Z` and `.demo/artifacts/story-director-qwen-20260921T135529Z`.
- Script Context Integration v1: DONE (wiring; no schema/migration, no DB/job/API/provider change, no model call). `GenerateScriptJobPayload` gained optional `creativeDirection`, `storyPlan`, `characterStates`, `worldStates` (backward compatible: older payloads deserialize with nulls). `GenerateScriptJobHandler` now injects the existing `IStoryContextBuilder`; when the payload carries the validated planning inputs it projects a whole-story `StoryContext` (via `StoryContextRequest`) and passes it to the existing `GenerateScriptPrompt`, which renders the compact, already-filtered context (ordered beats, only referenced characters/worlds, optional state) plus Script-ownership boundaries (exact spoken words; no camera/shot/lens/generation command/provider/model/path output, no plan replacement). Payload without narrative context keeps the previous prompt/behavior unchanged. Whole-story scope is used because the Script result is one ordered `sections[]`; the prompt instructs one section per beat in order. `StoryPlanParser`/`StoryPlanValidator`/`StoryPlan`/`NarrativePattern`/DI/Story Director untouched. Real-Qwen Script validation NOT yet run.
- Known integration debt: `IStoryDirector` is synchronous and `QwenStoryDirector` bridges the async model call with `.GetAwaiter().GetResult()`. Before Story Director is wired into production job/API orchestration, the synchronous `IStoryDirector` boundary must be revisited (not part of Script integration).
- Real-Qwen Script Generation Validation Harness v1: DONE (local harness + script only; no production change, no schema/migration, no DB/job/API, no media). `.demo/ScriptQwenValidation` drives the PRODUCTION `GenerateScriptJobHandler` (real `StoryContextBuilder` decorated only to capture the projected context, real `GenerateScriptPrompt`, real `OllamaTextGenerator`, real `GenerateScriptResult`) with an in-memory `IContentProjectReader` over the five validated real CreativeDirection (`.demo/artifacts/creative-director-qwen-20260921T125832Z`) and StoryPlan (`.demo/artifacts/story-director-qwen-20260921T135529Z`) pairs; it reports JSON/schema/concept/audience/section-count/beat-alignment/hook/closing/script-boundary/storyboard/impl-leak/output/latency/narrative/RESULT and preserves artifacts under gitignored `.demo/artifacts/script-qwen-<timestamp>/`. One-command `scripts/e2e-script-qwen.sh` preflights Ollama + configured model, verifies 5/5 creative + 5/5 story inputs, builds, runs a 12-check self-check, then the five cases. Real Script validation is USER-RUN; Character/World Bible grounding is NOT exercised while the real StoryPlans carry empty refs.
- Script Spoken-First Refinement v1: DONE (prompt-only production change + harness semantics/coverage; no schema/StoryContext/StoryPlan/Creative/Story Director change). Real-Qwen Script evidence (`.demo/artifacts/script-qwen-20260921T141627Z`) showed two classes: (A) concept/audience hard-FAIL was a token-overlap false negative (anime concept 33%, pet concept 22%, pet audience 0%, while premise and protagonist were preserved), and (B) real non-spoken leakage — `openingHook` copied `Close-up ...` from the harness-synthesized `Idea hook` (CreativeTreatment.hookTreatment) and the preschool `closing` copied `ding sound / sparkle effect` from a real StoryPlan beat purpose (inherited from CreativeTreatment.endingTreatment). Fix: `GenerateScriptPrompt` now states that openingHook, every narration, and closing are the exact spoken words, headings are labels, and forbids camera/shot/lens/editing/acting/sound-effect/visual-effect/scene-direction inside spoken fields while requiring translation of creative context into spoken language. Harness: concept/audience lexical overlap is now a REVIEW signal (never FAIL); boundary scanners examine only spoken fields (hook + every narration + closing; headings excluded); added evidence-backed sound/visual-effect patterns. Character/World grounding remains NOT exercised.
- Script Spoken-Content Boundary Refinement v2: DONE (prompt-only; no schema/StoryContext/StoryPlan/Creative/Story Director change; harness unchanged). Real-Qwen evidence (`.demo/artifacts/script-qwen-20260921T142747Z`, 2 PASS / 3 REVIEW / 0 FAIL) showed the only concrete production issue was case 3 preschool: narration `A bright ding sounds, and a sparkle effect appears...` copied from the beat purpose `...accompanied by a final ding sound and sparkle effect...` (inherited from CreativeTreatment.endingTreatment) rendered in the prompt. `GenerateScriptPrompt` now states that the idea hook/angle, creative guidance, and beat purposes may mention sound/visual cues, effects, or transitions (`ding sound`, `sparkle effect`, `boing`, `music rises`, `screen flashes`) as CONTEXT to translate, never as words to narrate; requires translating the narrative intention into spoken words (with a worked example); and forbids sound-effect/visual-effect/SFX/music/overlay/scene-direction instructions inside spoken fields, while allowing spoken descriptions of events a listener can hear or see (`the room suddenly goes quiet`). Beat alignment and the existing camera/storyboard rule are preserved. The harness `ScriptBoundary`/`StoryboardBoundary`/`ImplementationLeak` and concept/audience REVIEW logic were NOT modified — the effect-direction detection correctly caught the case. Concept/audience REVIEW remains intentional human-review behavior.
- Real-Qwen chain status: Creative Director, Story Director, and Script generation all validated (Script final acceptance `.demo/artifacts/script-qwen-20260921T143607Z` = 3 PASS / 2 REVIEW / 0 FAIL; the REVIEWs are human lexical concept/audience signals only, with no boundary/leak failures).
- Storyboard Context Integration v1: DONE (application-layer wiring; no schema/migration, no DB/job/API change, no model call). `GenerateStoryboardJobPayload` gained optional `creativeDirection`/`storyPlan`/`characterStates`/`worldStates` (backward compatible: older payloads deserialize with nulls). `GenerateStoryboardJobHandler` now injects the existing `IStoryContextBuilder`; when the payload carries the validated planning inputs it projects a whole-story `StoryContext` and passes it — together with the approved `GenerateScriptResult` spoken reference — to `GenerateStoryboardPrompt`. The prompt renders the compact context (concept, ordered beats with the "one section per beat, same order" mapping, only referenced characters/worlds, optional state) and now explicitly allows engine-neutral visual/camera/framing/composition/transition intent while forbidding provider/model/node-graph/render-script/path/URL/command detail and forbidding Script rewriting. `GenerateStoryboardResult { title, scenes[] { heading, visual } }` is unchanged (scenes stay ordered and aligned to script sections/beats). A payload without narrative context keeps the previous prompt/behavior. Character/World Bible context is consumed but NOT end-to-end validated (no refs in the real plans). Async Story Director debt remains.
- Real-Qwen Storyboard Validation Harness v1: DONE (local harness + script only; no production change, no schema/migration, no DB/job/API, no media). `.demo/StoryboardQwenValidation` drives the PRODUCTION `GenerateStoryboardJobHandler` (real `StoryContextBuilder` decorated only to capture the projected context, real `GenerateStoryboardPrompt`, real `OllamaTextGenerator`, real `GenerateStoryboardResult`) with an in-memory `IContentProjectReader` and an in-memory approved `ReviewedScript` (no DB), over the five validated real CreativeDirection / StoryPlan / Script artifact triples (not regenerated); it reports JSON/schema/title/scenes/script-rewrite/implementation-leak/output/latency plus human-review StoryAlignment/ScriptAlignment/VisualQuality and informational Beats/Sections/Scenes counts, with concise beat/section/scene previews. One-command `scripts/e2e-storyboard-qwen.sh` preflights Ollama + the configured model, verifies 5/5 + 5/5 + 5/5 inputs, builds, runs a 12-check self-check, then the five cases. Real Storyboard validation is USER-RUN. Because `StoryboardScene` has no beatId/sectionId and multiple scenes per beat are allowed, scene↔beat alignment is human-reviewed (never a hard rule). Character/World Bible grounding remains NOT exercised.
- Character + World Bible Grounding Foundation v1: DONE (application-layer, deterministic; no schema/migration, no DB/job/API, no model call, no media). New `StoryPlanGrounder` applies an explicit per-beat map (`StoryPlanGroundingRequest` + `StoryBeatGrounding { BeatId, CharacterRefs, WorldRefs }`) to an existing `StoryPlan` and returns a NEW plan; it replaces refs only on listed beats (unlisted beats keep their existing refs) and preserves id/version/sourceConcept/pattern(+version)/targetDuration/beat count/beat ids/order/role/purpose/importance/duration/continuity. Refs are validated through the existing `ICharacterBibleRegistry`/`IWorldBibleRegistry` (malformed ids via value-object `TryParse`); unknown beats, duplicate beat assignments, and unknown refs fail with stable `StoryPlanGroundingErrorCodes`, and the result is re-validated through the existing `StoryContinuityValidator`. No global/default fallback, and the input plan is never mutated. Grounded plans feed the existing `StoryContextBuilder`, so `StoryContext`/Script/Storyboard need no change. `StoryPlanGrounder` is registered in DI. No AI Bible generation yet; no visual-consistency claim; async Story Director debt unchanged.
- Qwen Story Bible Planner v1: DONE (application-layer AI planning proposal; no schema/migration, no DB/job/API, no media). New async `IStoryBiblePlanner` / `QwenStoryBiblePlanner` takes a `StoryBiblePlanningRequest` (CreativeDirection + StoryPlan), builds a deterministic prompt (`QwenStoryBiblePlannerPrompt`), makes ONE `IAiTextGenerator` request, and returns a validated `StoryBiblePlan { CharacterBibles, WorldBibles, BeatGroundings }`. `StoryBiblePlanParser` strictly parses the JSON (unknown members rejected) and validates deterministically, reusing `CharacterBibleValidator`/`WorldBibleValidator`: valid/unique character and world ids, relationships resolving within the proposal, beat ids existing in the supplied plan, no duplicate beat groundings, and every grounding ref resolving to a proposed bible (runtime registries are not required). Stable identity vs mutable state is enforced (no emotion/pose/lighting in bibles), asset references must be empty, and exact id reuse is required (never id + version). No side effects: it never registers bibles, never calls `StoryPlanGrounder`, never mutates the plan, never persists. The proposal → explicit registration → `StoryPlanGrounder` → `StoryContextBuilder` path is proven in tests. Real-Qwen Bible validation NOT run yet; state planning remains separate; visual consistency not claimed; async Story Director debt unchanged.
- Real-Qwen Story Bible Planner Validation Harness v1: DONE (local harness + script only; no production change, no schema/migration, no DB/job/API, no media). New `.demo/StoryBibleQwenValidation` drives the PRODUCTION `IStoryBiblePlanner` → `QwenStoryBiblePlanner` over the real `OllamaTextGenerator` (recording decorator captures prompt/raw response/latency) for exactly two already-validated cases (`anime-story`, `preschool-3d-story`), loading the real CreativeDirection (`.demo/artifacts/creative-director-qwen-20260921T125832Z`) and StoryPlan (`.demo/artifacts/story-director-qwen-20260921T135529Z`) through production parsers (no upstream rerun). Phase 1 validates JSON/schema/character-world counts/id validity+uniqueness/relationships/groundings/ref resolution/implementation leak and adds stable-id reuse (`CharacterReuse`/`WorldReuse`) plus REVIEW-only identity-fragmentation, character/world identity-quality, and asset-reference signals (no semantic evaluator, no auto-merge). Phase 2 materializes deterministically: fresh in-memory registries → explicit register → existing `StoryPlanGrounder`; Phase 3 projects the grounded plan through the existing `StoryContextBuilder`, proving grounded refs resolve, unrelated entities stay absent, and every non-ref StoryPlan narrative field is preserved. One-command `scripts/e2e-story-bible-qwen.sh` preflights Ollama + configured model, verifies the two upstream artifact pairs, builds, runs a 15-check self-check, then the two cases; artifacts (creative-direction, story-plan-original, prompt, raw-response, story-bible-plan, grounded-story-plan, story-context, validation, summary) under gitignored `.demo/artifacts/story-bible-qwen-<timestamp>/`. Real Qwen run is USER-EXECUTED; Script/Storyboard are not rerun; state planning remains separate; visual consistency NOT claimed.
- Real-Qwen Story Bible Planner validation: PASSED 2/2 (anime-story + preschool-3d-story) at planning level; artifacts `.demo/artifacts/story-bible-qwen-20260921T161017Z` (recurring `student-01`/`study-room` and `child-01`/`playroom-01` reused across beats; refs resolve; grounder + StoryContext succeed; original StoryPlan immutable; AssetReferences empty). Pixel/visual consistency still NOT validated.
- Story Bible Entity Selection Refinement v1: DONE (harness + planner-prompt only; no schema/migration, no DB/job/API, no media, no model call). Real evidence (`.demo/artifacts/story-bible-qwen-20260921T171803Z`) showed two semantic-quality issues: the preschool WorldBible `playroom-01`/`The Playroom` was classified `environmentType=bedroom` although the narrative establishes a playroom, and the ordinary toy `plushie` was proposed both as `recurringProps` and as character `plushie-01` (role `supporting`, species `toy`). Prompt gained two generic rules: never invent a more specific environment classification than the story establishes (prefer the closest supported neutral type), and create a characterBible only for a persistent narrative character/agent — ordinary objects, scenery, tools, toys, furniture, and passive props stay in `recurringProps`; agency, not species, decides (so an autonomous AI antagonist stays legitimate). Extracted the small deterministic coherence logic into shared `.demo/Shared/BibleSemanticSignals.cs` (linked into both harness projects) and the story-bible runner now reports `WorldIdentityCoherence` and new `EntityRoleCoherence` as REVIEW-only dimensions; both never mutate or repair a proposal. Domain schemas/validators, Grounder, StoryContext, Script, Storyboard unchanged. Real rerun remains USER-RUN.
- Grounded Identity Validation Refinement v1: DONE (harness + planner-prompt only; no schema/migration, no DB/job/API, no media, no model call). Real evidence (`.demo/artifacts/grounded-content-qwen-20260921T162709Z`) showed a false-positive age contradiction: `CharacterGrounding` matched the bare term `aged` inside the hyphenated "preschool-aged child" (`\baged\b` treats `-` as a boundary). Fix: removed the overly generic `aged` term from the harness elderly-term list (kept `elderly`/`old man`/`old woman`/`senior`/`grey-haired`), so genuine contradictions still REVIEW while "preschool-aged" no longer does. Added a generic REVIEW-only `WorldIdentityCoherence` signal: it flags a proposed WorldBible whose environment type and id/displayName are two different compound environment nouns sharing a specific head (real evidence: `playroom-01`/`Playroom` with `environmentType=bedroom`), never repairs data, never fails. `QwenStoryBiblePlannerPrompt` gained one rule that all stable identity fields of a proposed character/world must describe the SAME identity and not contradict each other. Domain validators/schemas, Grounder, StoryContext, Script, Storyboard unchanged. Real rerun remains USER-RUN.
- Grounded Script + Storyboard Real-Qwen Validation Harness v1: DONE (local harness + script only; no production change, no schema/migration, no DB/job/API, no media). New `.demo/GroundedContentQwenValidation` rebuilds the grounded state deterministically per case from the saved Bible proposal + ORIGINAL StoryPlan (production `StoryBiblePlanParser` → fresh `CharacterBibleRegistry`/`WorldBibleRegistry` → explicit register → `StoryPlanGrounder` → `StoryContextBuilder`), then drives the PRODUCTION `GenerateScriptJobHandler` and `GenerateStoryboardJobHandler` with that SAME grounded state (same registry instances back both `IStoryContextBuilder`s) and the real `OllamaTextGenerator`, approving the generated Script in-memory (no DB). It reports per-phase JSON/schema/sections-vs-beats/boundaries/implementation leak plus new deterministic REVIEW signals `CharacterGrounding`/`WorldGrounding`/`IdentityPreservation` (lexical, human-authoritative), proves both phases projected the SAME character/world identity set, and verifies the original StoryPlan stayed immutable. One-command `scripts/e2e-grounded-content-qwen.sh` preflights Ollama + configured model, verifies the two upstream artifact mappings, builds, runs a 12-check self-check, then exactly two cases (4 model generations). Artifacts under gitignored `.demo/artifacts/grounded-content-qwen-<timestamp>/`. Real run is USER-EXECUTED; state planning and Asset Registry remain future; async Story Director debt unchanged; no visual/pixel consistency claim.
- Story Bible Value Contract Diagnosis + Fix v1: DONE (prompt-only production change; no schema/migration, no DB/job/API, no model call). Real-Qwen evidence (`.demo/artifacts/story-bible-qwen-20260921T154934Z`) showed two failures with syntactically valid JSON: anime `characterBibles[1].relationships[0]` was a bare character-id string (`["student-01"]`) instead of a relationship object, and preschool emitted natural prose/capitalized labels where token fields are required (`identity.role` = "Protagonist"). Root cause: `QwenStoryBiblePlannerPrompt` never distinguished token fields from prose fields, never showed relationship object shape, and its one-line contract example did not demonstrate nesting. Fix: `QwenStoryBiblePlannerPrompt` now has one compact genre-neutral canonical JSON example (separate id/version, normalized tokens, prose only in description fields, relationship/variant/location object shapes, empty assetReferences, stable reuse in beatGroundings) plus explicit TOKEN-field vs PROSE-field rules with concrete role/environmentType examples. Strict parser, unknown-property rejection, value-object IDs, Bible validators, Grounder, StoryContext, and proposal-only semantics are unchanged; the saved failing responses still fail identically under the strict contract.
- Grounded Validation Input + Script World Heuristic Fix v1: DONE (harness + runner only; no production prompt/domain/schema/DB/job/API/media change, no model call). `scripts/e2e-grounded-content-qwen.sh` no longer hardcodes a Story Bible timestamp: an explicit `STORY_BIBLE_ARTIFACT_DIR` override wins (legacy `AISTUDIO_STORY_BIBLE_DIRECTIONS` still honored), otherwise the newest COMPLETE `story-bible-qwen-*` artifact under `.demo/artifacts` is discovered, incomplete candidates are skipped rather than silently used, and no valid artifact fails clearly; the selected artifact and its source are printed in Preflight. Selection logic was extracted to `scripts/lib/story-bible-artifact-selection.sh` and covered by deterministic `scripts/tests/story-bible-artifact-selection-test.sh` (override wins / newest complete / skip incomplete / clear empty), so no real home artifact is required to test it. Script world validation now treats a missing world token as NEUTRAL: new `GroundingSignalScanner.ScriptWorldGrounding` surfaces REVIEW only for an explicit environment term no WorldBible claims (an unsupported relocation), so silence no longer downgrades Script `WorldGrounding` or `IdentityPreservation`; Storyboard keeps the strict `WorldGrounding` (visual authority) and `CharacterGrounding` is unchanged. Harness self-check 25/25; Release build 0 warnings/0 errors, `dotnet format --verify-no-changes` and `git diff --check` clean. Home real-Qwen Story Bible is already validated 2/2 at `.demo/artifacts/story-bible-qwen-20260922T003757Z` (anime PASS, preschool PASS, `WorldIdentityCoherence` PASS both, `EntityRoleCoherence` PASS both). The final grounded real-Qwen run passed 2/2 (PASS 2, REVIEW 0, FAIL 0) at `.demo/artifacts/grounded-content-qwen-20260922T133225Z`; planning is frozen.
- Next Milestone: USER runs `scripts/e2e-flux-identity-reference.sh` with one explicitly approved real PNG reference and reviews the three outputs for identity consistency. If that proof passes, add the smallest materialization/job-payload seam that selects a Bible reference and persists/passes its concrete pin into `GenerateSceneVisuals`; do not re-resolve latest at execution and do not broaden to DB/API/multiple references/Blender.
- PRD Navigation: read `docs/product/PRD_INDEX.md` first, then only the relevant `docs/product/parts/` or `docs/product/execution-context/` file (storyboard work starts at `docs/product/execution-context/STORYBOARD.md`; asset work at `docs/product/execution-context/ASSETS.md`; narration work at `docs/product/execution-context/NARRATION_TTS.md`; rendering/QA at `docs/product/execution-context/RENDERING_QA.md`).
