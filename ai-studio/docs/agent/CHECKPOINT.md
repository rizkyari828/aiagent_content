# Agent Checkpoint

Updated: 2026-09-18

- Repository Layout: Content Studio now lives in `ai-studio/`; run its build, test, Compose, and documentation commands from that directory. Application behavior is unchanged.

- Current Phase: P1.
- AI Infrastructure Foundation v1: DONE / RUNTIME VALIDATED / FROZEN.
- API Surface: existing ContentProject/job endpoints plus `POST|GET|PUT /api/content-projects/{contentProjectId}/script-review`, `POST /api/content-projects/{contentProjectId}/script-review/approve`, `POST /api/content-projects/{contentProjectId}/storyboard-jobs`, `POST /api/content-projects/{contentProjectId}/storyboard-jobs/{jobId}/assets`, `GET /api/content-projects/{contentProjectId}/assets`, `POST /api/content-projects/{contentProjectId}/storyboard-jobs/{jobId}/narration`, `GET /api/content-projects/{contentProjectId}/narration`, `POST /api/content-projects/{contentProjectId}/storyboard-jobs/{jobId}/subtitle`, `GET /api/content-projects/{contentProjectId}/subtitle`, `POST /api/content-projects/{contentProjectId}/storyboard-jobs/{storyboardJobId}/render-jobs`, and `POST /api/content-projects/{contentProjectId}/render-jobs/{renderJobId}/qa-jobs`.
- Validated Flow: ContentProject -> GenerateIdea durable PostgreSQL Job -> Worker -> local Ollama -> structured `Job.Result` -> HTTP retrieval. The API returned `completed` with all six result fields and no worker/lease internals; PostgreSQL confirmed `Succeeded` with a persisted result.
- Runtime Compatibility: `qwen3.6:27b-q4_K_M` consistently failed JSON syntax validation with `ai_malformed_response`; the earlier coding-model smoke failed identically. `qwen3.8:27b-q4_K_M` completed through the existing retry policy after one timeout and one malformed response, with no application change.
- Phase 0 Local Developer AI: Ollama `0.34.1` is active as the WSL2 system service at `http://127.0.0.1:11434`; `OLLAMA_CONTEXT_LENGTH=49152` is verified. Qwen Code is currently `0.24.0` (the prior checkpoint recorded `0.23.4`) and uses `Qwen Code -> Anthropic-compatible provider -> Ollama`.
- Model Roles: developer coding model is `qwen3.6:27b-coding`; validated Content Studio runtime model is `qwen3.8:27b-q4_K_M`. The development configuration now explicitly requests `qwen3.8:27b-q4_K_M`; environment-variable model overrides remain supported.
- Qwen Routing: developer-productivity only. Use Qwen for bounded, repetitive, localized, low-risk work; use Codex for architecture, security/authorization, concurrency, migrations, durable jobs, destructive operations, ambiguous cross-cutting work, and complex debugging. Human diff review is mandatory before committing Qwen changes; do not build a compatibility platform around Qwen/Ollama.
- GenerateScript Slice: PRD v0.9.2 classifies idea/script drafts as A1 automatic and orders `Validate GenerateIdea -> GenerateScript -> Manual Script Review`; no Idea Approval gate is required. GenerateScript now accepts a selected structured idea, runs as the existing durable job type, validates a structured script, persists it in `Job.Result`, and exposes it through the existing job query. No schema/migration or durable-job semantic change.
- Manual Script Review/Edit: DONE. A completed GenerateScript `Job.Result` remains immutable AI evidence. `reviewed_scripts.content` is the canonical human-reviewed structured script, uniquely keyed per ContentProject and bound to `SourceJobId`; edits increment `Revision` only when canonical content changes, `Draft -> Approved` is the only transition, approval is idempotent, and approved content cannot be edited.
- Qwen Patch Review: APPROVED and committed as `f24f770`; Qwen `0.24.0` clears its file-read cache after auto-compaction, and future Qwen patches still require human/Codex review.
- Basic Storyboard: DONE. `GenerateStoryboard` reuses the existing durable job type with no schema/migration change. The canonical input is the `Approved` `ReviewedScript` (`reviewed_scripts` row), read directly and never from a historical GenerateScript `Job.Result`. Missing project, missing reviewed script, and draft/unapproved script are rejected at enqueue and in the handler (`content_project_not_found`, `storyboard_script_not_found`, `storyboard_script_not_approved`). Result is an immutable structured `Job.Result` (`title`, ordered `scenes[]` of `heading` + `visual`) exposed through the existing `GET /api/jobs/{jobId}`.
- Basic Asset Collection: DONE. Manual registration associates a local file under an approved `Assets:RootPath` with one storyboard scene, using the completed immutable GenerateStoryboard `Job.Result` as input; no external providers or generation. New additive `scene_assets` table/migration `20260918041620_AddSceneAssets` stores only canonical metadata (project, source job, scene index, type, relative path, byte size, SHA-256 content hash, origin, source/creator/license/retrievedAt, createdAt) and never file bytes. One asset per project scene (unique index); external assets require source and license. Path handling rejects rooted/absolute paths, `.`/`..` segments, Windows drive prefixes, and any path that traverses a symbolic link, and every candidate must resolve inside the configured root. Missing project/job/scene/file, duplicate scene, invalid hash, and missing provenance are rejected with stable codes.
- Basic Narration: DONE. Video #1 keeps narration manual; PRD v0.9.2 defers the TTS model, so no TTS/voice-cloning/STT was added. One `narration_tracks` row per ContentProject (additive migration `20260918042508_AddNarrationTracks`) stores only metadata (source storyboard job, relative path, byte size, SHA-256 content hash, origin, source/creator/license/retrievedAt, createdAt), never audio bytes. Registration is gated on an existing project and a completed GenerateStoryboard `Job.Result`, then reuses the existing `IAssetFileStore` for approved-root path safety and integrity hashing. Duplicate narration, missing/invalid storyboard, missing file, root escape, and missing external provenance are rejected with stable codes.
- Basic FFmpeg Composition: DONE. New durable `JobType.RenderVideo` produces one playable MP4 at deterministic `renders/{contentProjectId}/{storyboardJobId}.mp4` under the approved `Assets:RootPath`; no new table/schema (render evidence lives in `Job.Result`: output path, SHA-256, byte size, probe duration, dimensions, storyboard job, narration track, scene count, narration hash). Enqueue and handler both gate on project, a completed `GenerateStoryboard` result, one asset per scene bound to that storyboard, and that storyboard's narration. Handler re-reads every input through `IAssetFileStore` and rejects any file whose SHA-256 no longer matches its recorded hash. External process execution sits behind the single `IProcessRunner` boundary (`SystemProcessRunner` uses `ProcessStartInfo.ArgumentList`, no shell, bounded by `Rendering:TimeoutSeconds`; `FfmpegVideoRenderer` maps start failure/timeout and validates args). The renderer writes to a temp file, probes the result with `ffprobe` (video + audio stream, duration > 0), then atomically promotes it. Composition is minimal: scale/pad to `Rendering:Width`x`Height`, `fps`, one image/video input per scene, `concat`, `libx264` + `aac`, `+faststart`.
- Basic Subtitles: DONE. The v0.9.2 NOW/Video #1 path includes Subtitle, so an optional operator-provided `.srt` is supported. One `subtitle_tracks` row per ContentProject (additive migration `20260918081522_AddSubtitleTracks`) stores only metadata (source storyboard job, relative path, byte size, SHA-256, origin, source/creator/license/retrievedAt, createdAt), never bytes. Registration mirrors narration: project + completed GenerateStoryboard gate, `.srt`-only, external provenance, duplicate/format/missing-file/root-escape rejection, reusing `IAssetFileStore`. Rendering embeds the matching track as soft subtitles (`-i` input + `-map N:s:0` + `-c:s mov_text`) when its `SourceJobId` matches the render storyboard; a missing or mismatched subtitle is ignored, a tampered one is rejected before render. `Job.Result` records `subtitleTrackId`/`subtitleContentHash` when present.
- Basic Render Timing: equal scene duration = narration duration / scene count (marked with a `ponytail:` comment in `FfmpegCommandPlan`). No per-scene alignment or transition system was added.
- Final Video QA: DONE. New durable `JobType.FinalVideoQa` validates a completed `RenderVideo` `Job.Result` against its promoted MP4 with no new table/schema. The handler re-reads the artifact through `IAssetFileStore` (path safety, non-empty, SHA-256) and rejects a stored hash mismatch, then probes with the shared `IMediaInspector`/`FfprobeMediaInspector` process boundary (the renderer now reuses it instead of duplicating ffprobe parsing). It enforces duration > 0, video stream, audio stream, positive dimensions, and the expected subtitle stream when the render result recorded one. Result is an immutable `FinalVideoQaResult` `Job.Result` (output path, content hash, duration, dimensions, stream presence); failures throw stable `qa_*` `JobExecutionException` codes (`qa_artifact_missing`, `qa_hash_mismatch`, `qa_probe_failed`, `qa_duration_invalid`, `qa_video_stream_missing`, `qa_audio_stream_missing`, `qa_subtitle_stream_missing`, plus gating codes). Deterministic checks only; no AI/quality scoring. Enqueue is `POST /api/content-projects/{contentProjectId}/render-jobs/{renderJobId}/qa-jobs`. `PlaceholderJobHandler` now excludes `RenderVideo`/`FinalVideoQa`, fixing a latent `SingleOrDefault` dispatch overlap.
- Validation: Release build 0 warnings/0 errors; 59 focused Rendering tests (including Final QA handler/workflow/contracts and `FfprobeMediaInspector`), 18 focused Subtitle tests, and 4 EF model tests pass; 10 PostgreSQL persistence tests fail for the known missing-database environment (not a regression). `git diff --check` clean. Real Final QA smoke PASS on `ffmpeg`/`ffprobe` 9.0.1 through the real `SystemProcessRunner`: a synthetic video+audio+subtitle MP4, real SHA-256 integrity check, and real ffprobe produced `PASS` with 320x240 h264, aac audio, and `mov_text` subtitle; the temporary fixture was deleted. `global.json` pins `10.0.401`, which is now installed; `global.json` was not modified.
- Video #1 E2E: DONE. One real ContentProject (`01b7f0ef-e749-4e1d-bbad-a93d016b2be2`) executed the existing slice: GenerateIdea (completed after 1 retry) -> GenerateScript (initially failed `ai_timeout` x3 at the 120s client timeout) -> approved ReviewedScript -> GenerateStoryboard (8 scenes) -> 8 scene assets -> narration -> subtitle -> RenderVideo (24s 1280x720 h264/aac/mov_text MP4) -> FinalVideoQa (pass). Product AI stayed local (`qwen3.8:27b-q4_K_M`); no paid/external provider. Config correction: `Ollama:TimeoutSeconds` 120 -> 600 (the model runs near 9 tokens/second, so the script 3000-token budget needs more than 120s; the existing `1..600` validation bound already anticipated this). Artifact preserved at `src/AIStudio.Api/assets/renders/01b7f0efe7494e1dbbada93d016b2be2/1526be4b6d434f54999782cc368a1990.mp4`. Full suite 251/251 pass with the DB connection string set. `FinalVideoQa` result is persisted but not yet deserialized by `GET /api/jobs/{jobId}`.
- Next Milestone: manual publishing.
- PRD Navigation: read `docs/product/PRD_INDEX.md` first, then only the relevant `docs/product/parts/` or `docs/product/execution-context/` file (storyboard work starts at `docs/product/execution-context/STORYBOARD.md`; asset work at `docs/product/execution-context/ASSETS.md`; narration work at `docs/product/execution-context/NARRATION_TTS.md`; rendering/QA at `docs/product/execution-context/RENDERING_QA.md`).
