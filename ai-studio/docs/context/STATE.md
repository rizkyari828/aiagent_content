# Work state

Updated: 2026-09-22.

## Current milestone

Identity Asset Contract v1 DONE (HOME code-only milestone): stable key `(AssetReferenceId, IdentityAssetVersion)`; optional authored `AssetReference.Version`; explicit Approved-only resolution to a concrete pinned reference; in-memory metadata/version/approval registry; local byte/hash/size store with Infrastructure-private deterministic paths under the existing approved Assets root. Draft -> Approved is the only lifecycle and Approved metadata/key bytes are immutable (changed content requires a new version). Application carries no storage path/key/URL or provider configuration. No DB/API/job/provider/media/AI integration.

Visual Direction + Multi-Engine Choreography v1 DONE: deterministic director/router + animation primitives/choreography, AnimatedSvg engine, FLUX motion treatment, and per-scene visual invalidation; canonical production renders across Blender + Manim + AnimatedSvg + FLUX.

Production Orchestration v2 DONE: durable `GenerateAudio` (VoxCPM2 narration + ACE-Step BGM + `IAudioMixer` master, retry-safe stage reuse) and `RenderVideo` consuming the mastered audio.

Video #1 is DONE, including the durable animated visual pipeline: SceneVisualPlanner v2, SVG + Manim engines, and narration-aware durable timing parity. Audio Mixing / Mastering v1 is DONE as a standalone CPU FFmpeg service (no durable job/endpoint). Audio Generation v1 is DONE: local VoxCPM2 speech and ACE-Step music provider boundaries, disabled by default, no job/endpoint wiring yet.

Capability Registry v1 DONE (office code-only milestone): a deterministic, metadata-only `ICapabilityRegistry` discovers capabilities and resolves trusted providers from compile-time registrations plus existing enablement options, with explicit `CapabilityNotImplemented` vs `ProviderDisabled` gaps. It is not wired into production yet; `SceneVisualRouter` and all handlers are unchanged. Live runtime health checks are deferred.

Production Recipe v1 DONE (office code-only milestone): a content format is declarative DATA (`ProductionRecipe` = stable id + version + capability requirements), resolved deterministically against `ICapabilityRegistry` into `FullySupported` / `SupportedWithFallbacks` / `Unsupported` with per-requirement providers, fallbacks and gaps. Two seed recipes (`tech-explainer`, `motion-comic`) prove generality. It is not wired into production yet; all durable jobs and handlers are unchanged.

Concept Registry v1 DONE (office code-only milestone): a content concept is declarative DATA (`ConceptManifest` = descriptive metadata + a reference to a trusted recipe), validated structurally and resolved through the recipe resolver into `Ready` / `ReadyWithFallbacks` / `Unsupported`, with a missing recipe reported distinctly from an unavailable capability. Two seed concepts prove generality and clean JSON shape. It is not wired into production and no LLM is involved.

Creative Director v1 DONE (office code-only milestone): a planning layer above Concept Registry turns an approved idea into a `CreativeDirection` (`ConceptManifest` + `CreativeTreatment`) using a deterministic prompt and the existing `IAiTextGenerator` boundary; no LLM is called here. It does not replace or duplicate Solution Idea / GenerateIdea, does not auto-register concepts, and does not start production.

Story Director Foundation v1 DONE (office code-only milestone): a general, data-driven `AIStudio.Application.Stories` layer turns a `CreativeDirection` into a declarative `StoryPlan` shaped by a `NarrativePattern`. New narrative patterns and beat roles are DATA — no director subclass or enum. The v1 director is deterministic structural scaffolding; it calls no model and does not generate the idea, choose the creative treatment, write dialogue, or produce shot detail. It sits alongside the existing pipeline.

Character + World Bible v1 DONE (office code-only milestone): a general, engine-neutral `AIStudio.Application.Bibles` layer defines stable narrative identity for `StoryPlan` `characterRefs`/`worldRefs`. Identity and mutable scene state are separate; new characters/worlds/categories are DATA. Bible registries remain in-memory with no DB; reusable asset metadata/bytes are owned separately by Identity Asset Contract v1.

Story Context Builder v1 DONE (office code-only milestone): a deterministic, AI-free `AIStudio.Application.StoryContext` layer projects only the narrative inputs a story (or one beat) actually references into a compact, consumer-neutral `StoryContext` for future Script/Storyboard stages. Token efficiency comes from relevance filtering, not truncation. No DB, no asset storage, no production routing.

Extractable Domain Modules + Reusable Capability Rules DONE (documentation + development-guardrail milestone): `ai-studio/AGENTS.md` now carries the modular/domain boundary hard rules, the PRD gained the labeled post-freeze addendum `docs/product/parts/13_EXTRACTABLE_MODULE_BOUNDARIES.md` with ADR-018/ADR-019, and cheap assembly-level architecture tests enforce the layer directions. No source/runtime/package/schema/service-split change; no Stock implementation, broker, DB split, or remote client was introduced.

## Implemented

- Identity Asset Contract v1: new provider-neutral `AIStudio.Application.IdentityAssets` contracts (`IdentityAssetVersion`, Draft/Approved status, minimal identifier-only provenance, `IdentityAsset`, validator/issues, `IIdentityAssetRegistry`, `IIdentityAssetStore`, blob facts, Approved-only resolver + stable not-found/not-approved issues). Registry owns metadata, monotonic `NextVersion`, registration, approval/`ApprovedAt`, exact/latest/latest-approved lookup; store owns bytes/SHA-256/size. `LocalIdentityAssetStore` delegates approved-root containment, traversal/symlink safety, and atomic writes to `IAssetFileStore`, exposes no path, separates ids/versions collision-free, and rejects changed bytes at an existing key. `AssetReference.Version` is additive/optional; missing remains valid authoring JSON, while materialization resolves latest Approved once and returns a concrete pin. StoryContext preserves an authored version but does not resolve it. Provider requests, `SceneAsset`, DB/schema, API, jobs, ComfyUI/FLUX/Blender, and production wiring are unchanged.

- Grounded Validation Input + Script World Heuristic Fix v1 (harness + runner only; no production prompt/domain/schema/DB/job/API/media change, no model call): `scripts/e2e-grounded-content-qwen.sh` selects the Story Bible artifact without a hardcoded timestamp — explicit `STORY_BIBLE_ARTIFACT_DIR` wins (legacy `AISTUDIO_STORY_BIBLE_DIRECTIONS` still honored), else the newest COMPLETE `story-bible-qwen-*` under `.demo/artifacts`, skipping incomplete candidates and failing clearly when none is valid; the selected path + source print in Preflight. Selection lives in `scripts/lib/story-bible-artifact-selection.sh` with deterministic `scripts/tests/story-bible-artifact-selection-test.sh` coverage. Script world validation now treats an absent world token as neutral (new `GroundingSignalScanner.ScriptWorldGrounding`); only an explicit environment term no WorldBible claims REVIEWs, so `WorldGrounding`/`IdentityPreservation` no longer fail on silence. Storyboard keeps strict `WorldGrounding`; `CharacterGrounding` unchanged. Harness self-check 25/25. Home real-Qwen Story Bible already validated 2/2 at `.demo/artifacts/story-bible-qwen-20260922T003757Z`; the final grounded real-Qwen run remains HOME-only and the planning freeze waits for it.

- Qwen Story Bible Planner v1 (application-layer AI proposal; no schema/migration, no DB/job/API, no media): the async `IStoryBiblePlanner`/`QwenStoryBiblePlanner` consumes `CreativeDirection` + `StoryPlan`, builds a deterministic prompt, makes one `IAiTextGenerator` request, and returns a strictly parsed + deterministically validated `StoryBiblePlan { CharacterBibles, WorldBibles, BeatGroundings }`. Validation reuses `CharacterBibleValidator`/`WorldBibleValidator` and resolves beat refs against the bibles in the same proposal (no runtime registries needed). It has no side effects: no registration, no grounding application, no plan mutation, no persistence. The proposal→register→`StoryPlanGrounder`→`StoryContextBuilder` path is proven. Real-Qwen Bible validation not yet run; state planning remains separate; visual consistency not claimed.

- Real-Qwen Story Bible Planner Validation Harness v1 (local harness + script only; no production change, no schema/migration, no DB/job/API, no media): `.demo/StoryBibleQwenValidation` drives the real production `IStoryBiblePlanner` → `QwenStoryBiblePlanner` over the real `OllamaTextGenerator` (a recording decorator captures prompt/raw response/latency) for exactly two already-validated cases (anime-story, preschool-3d-story), loading the real CreativeDirection (`.demo/artifacts/creative-director-qwen-20260921T125832Z`) and StoryPlan (`.demo/artifacts/story-director-qwen-20260921T135529Z`) through the production parsers without rerunning upstream stages. It reports JSON/schema/character-world counts/id validity and uniqueness/relationships/groundings/ref resolution/implementation leak, plus stable-id reuse (`CharacterReuse`/`WorldReuse`) and REVIEW-only fragmentation + identity/state-leak signals (no semantic evaluator, no auto-merge), then materializes deterministically through fresh in-memory registries → explicit registration → the existing `StoryPlanGrounder` → the existing `StoryContextBuilder`, proving grounded refs resolve and every non-ref StoryPlan narrative field is preserved. `scripts/e2e-story-bible-qwen.sh` is the one-command USER-RUN entry point over two cases. Script/Storyboard are not rerun; state planning remains separate; visual consistency is not proven.

- Story Bible Entity Selection Refinement v1 (harness + planner-prompt only; no schema/migration, no DB/job/API, no media, no model call): real evidence `.demo/artifacts/story-bible-qwen-20260921T171803Z` showed preschool `playroom-01` classified `environmentType=bedroom` and the ordinary toy `plushie` proposed both as a `recurringProps` entry and as character `plushie-01`. `QwenStoryBiblePlannerPrompt` now states that an environment classification must not be invented beyond what the story establishes (prefer the closest supported neutral type) and that a characterBible is only for a persistent narrative character/agent whose identity/behavior continuity matters, while ordinary objects/toys/props stay in `recurringProps` (agency, not species, decides). Shared `.demo/Shared/BibleSemanticSignals.cs` now holds the small deterministic `WorldIdentityCoherence` and new `EntityRoleCoherence` REVIEW-only signals, linked into both validation harnesses; the story-bible runner reports both. Signals never mutate or repair a proposal. Domain schemas/validators, `StoryPlanGrounder`, `StoryContextBuilder`, Script, and Storyboard are untouched; real rerun remains USER-RUN.

- Grounded Identity Validation Refinement v1 (harness + planner-prompt only; no schema/migration, no DB/job/API, no media, no model call): the grounded-content harness no longer treats the hyphenated "preschool-aged child" as an age contradiction (the bare `aged` term was removed from its elderly-term list; `elderly`/`old man`/`old woman`/`senior` stay and still surface real contradictions), and it now emits a REVIEW-only `WorldIdentityCoherence` signal when a proposed WorldBible's environment type and id/displayName name different compound environments sharing a specific head (real evidence: `playroom-01`/`Playroom` vs `environmentType=bedroom`). `QwenStoryBiblePlannerPrompt` now states that all stable identity fields of a proposed character/world must describe the same identity and not contradict each other. Domain validators, schemas, `StoryPlanGrounder`, `StoryContextBuilder`, Script, and Storyboard are untouched; real rerun remains USER-RUN.

- Grounded Script + Storyboard Real-Qwen Validation Harness v1 (local harness + script only; no production change, no schema/migration, no DB/job/API, no media): `.demo/GroundedContentQwenValidation` rebuilds the grounded state deterministically per case from the saved Bible proposal plus the ORIGINAL StoryPlan (production parser → fresh registries → explicit register → `StoryPlanGrounder` → `StoryContextBuilder`) and then drives the real production `GenerateScriptJobHandler` and `GenerateStoryboardJobHandler` with that SAME grounded state (the same registry instances back both injected `IStoryContextBuilder`s) and the real `OllamaTextGenerator`, approving the generated Script in-memory (no DB). It emits per-phase JSON/schema/section-vs-beat/boundary/implementation-leak dimensions plus new lexical REVIEW-only grounding signals (`CharacterGrounding`, `WorldGrounding`, `IdentityPreservation`), proves Script and Storyboard projected the same character/world identity set as the canonical grounded context, and checks the original StoryPlan narrative fields stayed immutable. `scripts/e2e-grounded-content-qwen.sh` is the one-command USER-RUN entry point over exactly two cases (anime-story, preschool-3d-story; 4 model generations). Real run is USER-RUN; state planning and Asset Registry remain future; async Story Director debt unchanged; pixel/visual consistency is not claimed.

- Real-Qwen Story Bible Planner result: PASSED 2/2 at planning level (`.demo/artifacts/story-bible-qwen-20260921T161017Z`), with stable recurring identities reused across beats for anime (`student-01`, `study-room`) and preschool (`child-01`, `playroom-01`). Downstream Script/Storyboard consumption is validated by the grounded-content harness; pixels are not.

- Story Bible Value Contract Diagnosis + Fix v1 (prompt-only production change; no schema/migration, no DB/job/API, no media, no model call): the first real-Qwen story-bible run (`.demo/artifacts/story-bible-qwen-20260921T154934Z`) produced syntactically valid JSON that violated the domain contract in two ways — anime emitted `characterBibles[1].relationships[0]` as a bare character-id string (`"student-01"`) instead of a relationship object, and preschool emitted a capitalized prose label for the token field `identity.role` (`"Protagonist"`). `QwenStoryBiblePlannerPrompt` now carries one compact genre-neutral canonical JSON example (separate id/version, normalized tokens, prose confined to description fields, relationship/variant/location object shapes, empty `assetReferences`, stable-ID reuse in `beatGroundings`) and explicit TOKEN-vs-PROSE field rules with concrete `role`/`environmentType` examples. Parser, value-object IDs, Bible validators, `StoryPlanGrounder`, `StoryContextBuilder`, and proposal-only semantics are unchanged and still strict; the saved failing responses still fail identically. Real Qwen rerun is USER-EXECUTED.

- Character + World Bible Grounding Foundation v1 (application-layer, deterministic; no schema/migration, no DB/job/API, no model call, no media): new concrete `StoryPlanGrounder` applies an explicit per-beat assignment map to an existing `StoryPlan` and returns a NEW plan, replacing only `CharacterRefs`/`WorldRefs` on listed beats while preserving every other narrative property; unlisted beats keep their refs, there is no global/default fallback, and the input plan is never mutated. Refs are validated through the existing `ICharacterBibleRegistry`/`IWorldBibleRegistry` and the result is re-validated with the existing `StoryContinuityValidator`; unknown beats, duplicate assignments, and unknown refs fail with stable `StoryPlanGroundingErrorCodes`. Grounded plans flow into the existing `StoryContextBuilder` unchanged. No AI Bible generation yet; no visual-consistency claim. `StoryPlan`/`StoryBeat` schema unchanged.

- Real-Qwen Storyboard Validation Harness v1 (local harness + script only; no production change, no schema/migration, no DB/job/API, no media): `.demo/StoryboardQwenValidation` drives the production `GenerateStoryboardJobHandler` with a capturing decorator over the real `StoryContextBuilder`, the real `GenerateStoryboardPrompt`, the real `OllamaTextGenerator`, and the real `GenerateStoryboardResult`, fed by the five validated real CreativeDirection + StoryPlan + Script artifact triples (not regenerated) and an in-memory approved `ReviewedScript` (no DB). It reports JSON/schema/title/scenes/script-rewrite/implementation-leak/output/latency plus human-review StoryAlignment/ScriptAlignment/VisualQuality, with informational Beats/Sections/Scenes counts and beat/section/scene previews, preserving artifacts under gitignored `.demo/artifacts/storyboard-qwen-<timestamp>/`. `scripts/e2e-storyboard-qwen.sh` is the one-command USER-RUN entry point. Because `StoryboardScene` has no beatId/sectionId, scene↔beat alignment is human-reviewed and scene-count differences never fail. Character/World grounding remains not end-to-end validated.

- Storyboard Context Integration v1 (application-layer wiring; no schema/migration, no DB/job/API change, no model call): `GenerateStoryboardJobPayload` gained optional `creativeDirection`/`storyPlan`/`characterStates`/`worldStates`; `GenerateStoryboardJobHandler` injects `IStoryContextBuilder`, builds a whole-story `StoryContext` when the payload carries the validated planning inputs, and `GenerateStoryboardPrompt` renders the compact context (concept, ordered beats, only referenced characters/worlds, optional state) alongside the approved `GenerateScriptResult` spoken reference. The prompt now allows engine-neutral visual/camera/framing/composition/transition intent while forbidding provider/model/node-graph/render-script/path/URL/command detail and Script rewriting; the `GenerateStoryboardResult` contract is unchanged (ordered scenes aligned to script sections/beats). A payload without narrative context keeps the previous behavior. Character/World grounding is consumed but not end-to-end validated; async Story Director debt remains. Real-Qwen chain: Creative, Story, and Script all validated (Script 3 PASS / 2 REVIEW / 0 FAIL, REVIEW = lexical human signals only).

- Script Spoken-Content Boundary Refinement v2 (prompt-only; no schema/StoryContext/StoryPlan/Creative/Story Director change; harness unchanged): the real-Qwen Script run (`.demo/artifacts/script-qwen-20260921T142747Z`) showed the only concrete production issue was preschool narration `A bright ding sounds, and a sparkle effect appears...`, copied from a beat purpose inherited from CreativeTreatment.endingTreatment. `GenerateScriptPrompt` now treats idea hook/angle, creative guidance, and beat purposes as CONTEXT that may mention sound/visual cues, effects, or transitions to be translated into spoken words — never narrated as production instructions — and forbids sound-effect/visual-effect/SFX/music/overlay/scene-direction inside spoken fields while allowing spoken descriptions of events (`the room suddenly goes quiet`). Concept/audience low lexical overlap remains a REVIEW signal, not a failure; beat alignment and the camera/storyboard boundary rule are preserved.

- Script Spoken-First Refinement v1 (prompt-only production change; no schema/StoryContext/StoryPlan/Creative/Story Director change): the real-Qwen Script run showed concept/audience hard-FAIL were token-overlap false negatives, and that spoken fields copied production direction (`Close-up ...` in `openingHook` from the harness-synthesized idea hook = CreativeTreatment.hookTreatment; `ding sound / sparkle effect` in the preschool `closing` from a real StoryPlan beat purpose inherited from CreativeTreatment.endingTreatment). `GenerateScriptPrompt` now defines openingHook/sections[].narration/closing as the exact spoken words (headings are labels) and forbids camera/shot/lens/editing/acting/sound-effect/visual-effect/scene-direction inside them, requiring translation of creative context into spoken language. Harness: concept/audience lexical overlap is a REVIEW signal (never FAIL); boundary scans cover only spoken fields; added evidence-backed sound/visual-effect patterns. Character/World grounding remains untested.

- Real-Qwen Script Generation Validation Harness v1 (local harness + script only; no production change, no schema/migration, no DB/job/API, no media): `.demo/ScriptQwenValidation` drives the production `GenerateScriptJobHandler` with a capturing decorator over the real `StoryContextBuilder`, the real `GenerateScriptPrompt`, the real `OllamaTextGenerator`, and the real `GenerateScriptResult`, fed by the five validated real CreativeDirection + StoryPlan artifact pairs (not regenerated). It reports JSON/schema/concept/audience/section-count/beat-alignment/hook/closing/script-boundary/storyboard/impl-leak/output/latency/narrative/RESULT and preserves artifacts under gitignored `.demo/artifacts/script-qwen-<timestamp>/`. `scripts/e2e-script-qwen.sh` is the one-command USER-RUN entry point. Real-Qwen Script validation remains USER-RUN. Character/World Bible grounding is NOT exercised while the real StoryPlans carry empty refs (reported explicitly, never fabricated).

- Script Context Integration v1 (no schema/migration, no DB/job/API/provider change, no model call): `GenerateScriptJobPayload` gained optional `creativeDirection`/`storyPlan`/`characterStates`/`worldStates`; `GenerateScriptJobHandler` injects `IStoryContextBuilder`, builds a whole-story `StoryContext` when the payload carries the validated planning inputs, and `GenerateScriptPrompt` renders the compact, already-filtered context (ordered beats, only referenced characters/worlds, optional state) with Script-ownership/boundary rules. A payload without narrative context keeps the previous prompt/behavior unchanged. `StoryPlanParser`/`StoryPlanValidator`/`StoryPlan`/`NarrativePattern`/DI/Story Director are untouched. Real-Qwen Script validation NOT yet run. Real-Qwen Creative Director and Story Director are both validated 5/5 (Story after the boundary refinement). Async Story Director debt (`.GetAwaiter().GetResult()` bridge) must be revisited before job/API orchestration.

- Story Director Boundary Refinement v1 (prompt-only; no parser/validator/`StoryPlan`/`NarrativePattern` change, harness heuristic not weakened): real-Qwen Story Director validation produced 3 PASS / 2 REVIEW / 0 FAIL, with the two REVIEWs caused by storyboard-style wording (`camera`/`cut to` in anime-story, `close-up` in preschool-3d-story) that was carried in from the upstream CreativeDirection treatment fields (`endingTreatment`, `transitionStrategy`, `hookTreatment`) without an explicit translation rule. `QwenStoryDirectorPrompt` now marks the creative-direction fields as creative-intent CONTEXT, requires translating them into narrative events/state/purpose, forbids named cinematography/editing execution terms (close-up, medium/wide shot, camera, camera angle, lens/mm, pan, tilt, dolly, rack focus, cut to, hard cut, cross-dissolve, zoom, frame number, shot number, camera coordinates), and explicitly allows narrative visual state. Future catalog consideration (not this milestone): `problem-solution-short` is not ideal for anime mystery/reveal; consider a registered `mystery-reveal` pattern when anime production needs it.

- Real-Qwen Story Director Validation Harness v1 (local harness + script only; no production change, no schema/migration, no DB/job/API, no media): `.demo/StoryDirectorQwenValidation` resolves the production `IStoryDirector` via `StoryDirector:Mode=qwen` → `QwenStoryDirector` (real DI selection) with a recording `IAiTextGenerator` over the real `OllamaTextGenerator`, feeds the five real CreativeDirection artifacts (`.demo/artifacts/creative-director-qwen-20260921T125832Z`, not regenerated), and validates five caller-selected existing patterns (`explanatory-flow` for tech-explainer; `problem-solution-short` for anime/preschool/photoreal/pet). It reports JSON/schema/concept/pattern/beats/duration/continuity/refs/script/storyboard/impl-leak/output/latency/narrative/RESULT and preserves artifacts under gitignored `.demo/artifacts/story-director-qwen-<timestamp>/`. `scripts/e2e-story-director-qwen.sh` forces `StoryDirector__Mode=qwen` for its process only (app default stays deterministic). Real five-case Qwen run is USER-EXECUTED; not run by the agent.

- Qwen-backed Story Director v1 (small production capability; no schema/migration, no DB, no job/endpoint, no media): new `QwenStoryDirector` implements the existing `IStoryDirector` by building a deterministic prompt (`QwenStoryDirectorPrompt`) from the `CreativeDirection` + caller-supplied `NarrativePattern`, calling `IAiTextGenerator` (existing Ollama boundary), then parsing/validating via the existing `StoryPlanParser` + `StoryPlanValidator` (never bypassed), with no provider-specific or model coupling in the domain. New `StoryDirectorOptions` (`StoryDirector__Mode`, default `deterministic`) selects the implementation: deterministic → preserved `StoryDirector`, qwen → `QwenStoryDirector`; an unknown mode fails clearly and there is no silent fallback (a Qwen failure surfaces, no deterministic fallback). Qwen never selects the pattern; the supplied pattern is authoritative and the director pins `id`/`version`/`sourceConceptId`/`narrativePattern`/`targetDurationSeconds`. Creative Director real-Qwen validation PASSED 5/5 (`qwen3.8:27b-q4_K_M`, `.demo/artifacts/creative-director-qwen-20260921T125832Z`).

- Creative Director Real-Qwen Validation Harness v1 (local harness + script only; no schema/migration, no DB, no product/architecture change): new `.demo/CreativeDirectorQwenValidation` console harness reuses the production `ICreativeDirector` over the real `OllamaTextGenerator` (recording decorator captures prompt/raw response; no parallel pipeline). Validates 5 representative `ApprovedIdea` cases across JSON validity, schema, idea preservation (deterministic token-overlap heuristic), audience preservation (with model-vs-guardrail visibility), recipe id/status (valid / supported-with-fallback / unsupported / hallucinated), format/style, treatment printed for human review, implementation-leak scan, output size, latency, RESULT=PASS/REVIEW/FAIL. `scripts/e2e-creative-director-qwen.sh` sources `.env`, preflights Ollama + configured model, builds, runs an 11-check self-check, then the cases, preserving raw artifacts under gitignored `.demo/artifacts/creative-director-qwen-<timestamp>/`. Real multi-case Qwen run is USER-EXECUTED.

- Extractable Domain Modules + Reusable Capability Rules (documentation + guardrail only; no schema/migration, no package, no runtime behavior, no service split): formalizes "modular first, distributed when operationally justified" and "domain workflows stay specific; shared capabilities are extracted only after proven duplication". `ai-studio/AGENTS.md` gained a compact `## Modular and Domain Boundaries` rule block (no cross-module persistence/Infrastructure access; application-owned, transport-neutral serializable contracts/events; module-owned workers; domain-specific business models; proven-duplication-only capability extraction; no microservice infrastructure until an actual extraction). The canonical PRD gained the clearly labeled post-freeze addendum `docs/product/parts/13_EXTRACTABLE_MODULE_BOUNDARIES.md`, which is explicitly not derived from the frozen source and does not change its SHA-256; it covers modular-first/distributed-when-justified, co-located-but-remotely-extractable-by-contract (extraction replaces the transport adapter, not the business logic), domain-specific Content vs Stock models, proven-capability reuse, the illustrative and uncommitted future Content Intelligence / Stock Intelligence topology, deferred distributed microservices, and ADR-018 Extractable Module Boundaries plus ADR-019 Domain-Specific Workflows and Proven Capability Reuse. `PRD_INDEX.md` and `SECTION_MAP.md` index the addendum separately from source sections. `docs/development/DECISIONS.md` gained D-042/D-043 and `docs/development/ARCHITECTURE.md` links the ADRs. Enforcement is a new cheap reflection-only `tests/AIStudio.Tests/Architecture/ArchitectureBoundaryTests.cs` (Domain↛Application/Infrastructure/Api, Application↛Infrastructure/Api, Infrastructure↛Api; no architecture-testing package); namespace-level cross-module rules are documented as deferred. No Stock entity/crawler/service, message broker, service discovery, separate database, or remote client was added.

- Story Context Builder v1 (no schema/migration, no LLM, no production routing, no DB): a new application-layer `AIStudio.Application.StoryContext` layer sits between the trusted narrative inputs and future Script/Storyboard consumers. `StoryContextBuilder` (`IStoryContextBuilder`) consumes the existing `CreativeDirection`, `StoryPlan`, `ICharacterBibleRegistry`, and `IWorldBibleRegistry` (nothing is duplicated or mutated) and returns a `StoryContextBuildResult` with a compact `StoryContext` plus deterministic `StoryContextIssue`s. The projection has a concept summary (`StoryConceptContext`: id/title/audience/format/style/duration, no recipe/tags/description), the `CreativeTreatment` reused as context, a `StoryNarrativeContext` (plan id/version, pattern/version, target duration), only the referenced `StoryCharacterContext`s, only the referenced `StoryWorldContext`s, ordered `StoryBeatContext`s (purpose/order/role/importance/duration/refs/continuity; no dialogue or shot detail), optional `StoryStateContext` (caller-supplied `CharacterState`/`WorldState`, projected separately from stable identity), and the `BeatScope`. Selection is relevance-based and deterministic: referenced ids are deduplicated and sorted by id ordinal, beats by order then id, characters/worlds by id, relationships are filtered to included characters and sorted by target then type, asset metadata is excluded by default and, when explicitly requested, exposed only as id/purpose/variant. Whole-story builds include every beat; a request with `BeatId` builds beat-scoped context containing exactly the selected beat plus its transitive `continuityFrom` closure (no unrelated beats/characters/worlds). Reference validation reuses `StoryContinuityValidator` against a non-mutating plan copy scoped to the projected beats, so unresolved characters/worlds are reported (never silently dropped) with the shared continuity codes. Bible references resolve to the latest registered version deterministically. `AssetReferences` are never resolved to storage; no path, URL, provider, or command is exposed. The builder has no AI/provider/process dependency and does not mutate its inputs or the registries. Existing pipeline behavior is unchanged and no job/handler consumes `StoryContext` yet.

- Character + World Bible v1 (no schema/migration, no LLM, no production routing, no DB, no asset storage): a new application-layer `AIStudio.Application.Bibles` layer gives `StoryPlan` references authoritative definitions. A `CharacterBible` separates stable IDENTITY (`CharacterIdentity`: data-driven role/species/agePresentation/bodyStyle/hair/eyes plus free-text visualDescription and distinguishingTraits; `PersonalityTraits`; `BaselineVariant`; approved `Variants`; data-driven `Relationships`; engine-neutral `AssetReference`s) from scene-level `CharacterState` (`characterRef`, `variant`, emotion/pose/action, `worldRef`, `heldProps`). A `WorldBible` separates stable identity (`WorldIdentity`: environmentType, visualDescription, spatialTraits; recurring props; free-text continuity rules; optional named `WorldLocation`s; asset references) from `WorldState` (timeOfDay/weather/lighting/temporaryProps/notes). `CharacterBibleId`/`WorldBibleId`/`AssetReferenceId` are validated value objects (reusing the shared `StoryIdentifier` rule) and `AssetReference` carries an `AssetReferenceId`, optional identity-asset `version`, and data-driven `purpose`/`variant` — never a path, URL, command, or provider blob. `ICharacterBibleRegistry`/`CharacterBibleRegistry` and `IWorldBibleRegistry`/`WorldBibleRegistry` are trusted in-memory catalogs (validate on register, fail-fast invalid/duplicate id+version, deterministic id-then-version ordering, `Get`/`GetLatest`/`TryGet`/`TryGetLatest`) with no database, cache, hot reload, or executable plugin. `CharacterBibleValidator`/`WorldBibleValidator` are deterministic (identity/trait/variant/relationship/location/asset structure plus state token validity and approved-variant enforcement) and never score visual similarity or call a model. `StoryContinuityValidator` is additive: it reports unknown `StoryPlan` character/world references against the registries, without changing the Story Director or requiring bibles for existing plans. `BibleParser` parses strict JSON (unknown members rejected, so executable fields cannot be smuggled in) into validated bibles, with no speculative repair. v1 seeds no identity data; a new human/robot/animal/fantasy character or any world is data, not a C# subclass. Existing pipeline behavior is unchanged and no job/handler consumes bibles yet.

- Story Director Foundation v1 (no schema/migration, no LLM call, no production routing): a new application-layer `AIStudio.Application.Stories` layer turns a creative direction into a narrative progression. `StoryPlan` is declarative data — `StoryPlanId`/`StoryPlanVersion`, a `sourceConceptId` reference (never a duplicated `ConceptManifest`), a data-driven `NarrativePattern` id/version, `targetDurationSeconds`, and an ordered `StoryBeat[]`. `NarrativePattern` (id, version, displayName, beatSlots) and each `NarrativePatternBeatSlot` (role, purpose, durationWeight, required) are pure data, so a new format needs no C# type; `INarrativePatternRegistry`/`NarrativePatternRegistry` is a trusted in-memory catalog that validates on register, fails fast on invalid/duplicate id+version, orders deterministically by id then version, and supports `Get`/`GetLatest` — no DB, Redis, hot reload, or dynamic plugin loading. `StoryBeat` carries narrative intent only (`id`, `order`, data-driven `role`, `purpose`, data-driven `importance`, `targetDurationSeconds`) plus lightweight continuity references (`characterRefs`, `worldRefs`, `continuityFrom`) that are identifiers only — no character/world bible yet. `StoryPlanValidator` is deterministic: valid plan id/version, valid source-concept and pattern references, non-empty beats, unique beat ids and orders, positive beat durations, sum-of-beats within a relative duration tolerance, reference-token validity, `continuityFrom` known/non-self/acyclic, and (against a pattern) required-slot coverage. `StoryDirector` (`IStoryDirector`) is a deterministic v1 scaffold that maps each pattern slot to one ordered beat and shares the target duration by slot weight; it consumes only `INarrativePatternRegistry` (no AI or process dependency), throws stable `story_request_invalid`/`story_pattern_not_found`/`story_plan_invalid` codes, and a future Qwen-backed director will return the same `StoryPlan` shape. `StoryPlanParser` parses strict JSON (unknown members rejected, so executable fields cannot be smuggled in) into a `StoryPlan` and invokes the validator, preparing for future structured Qwen output without speculative repair. Seed patterns are only `problem-solution-short` and `explanatory-flow`; anime-horror and kids-song patterns register and direct as pure test data. Creative Director, Script, Storyboard, and every existing job/handler are untouched.

- Creative Director v1 (no schema/migration, no LLM call, no production routing): a new application-layer `AIStudio.Application.Creative` layer answers "how should an approved idea be expressed creatively and produced?" without absorbing idea generation. `ApprovedIdea` is a neutral boundary (optional `IdeaReference`, `Topic`, `Angle`, `Audience`, optional `Objective`, `HookPremise`, optional `SourceSummary`, optional `Language`, `CreativeConstraints`) with `ApprovedIdea.FromGenerateIdeaResult(...)` mapping the existing `GenerateIdeaResult` descriptively (Title→Topic, Angle→Angle, TargetAudience→Audience, Hook→HookPremise, Summary→SourceSummary, SuggestedFormat→preferred-format hint) and copying no job/infrastructure concerns; `ApprovedIdeaValidator` checks the required idea fields and validates the optional format/style/duration hints as data. The output `CreativeDirection` reuses the existing `ConceptManifest` (never forked) plus a small `CreativeTreatment` (`storyApproach`, `hookTreatment`, `pacing`, `visualStrategy`, `endingTreatment`, optional `tone`/`transitionStrategy`) that is descriptive data only — no final script, scene storyboard, Manim/Blender/ComfyUI prompt, or executable instruction. `CreativeDirectionParser` accepts valid JSON only, rejects malformed JSON and missing concept/treatment sections, maps the concept portion to `ConceptManifest` and reuses `ConceptValidator`, validates the treatment with `CreativeTreatmentValidator`, and does no speculative repair. `CreativePlanningContextProvider` projects the trusted registries into a SAFE summary — recipe id/version/display name, required capability ids, `Resolvable`, `UsesFallbacks`, and available/unavailable capability ids — with no provider class names, executable/model paths, scripts, or secrets. `CreativeDirectorPrompt` is deterministic and token-efficient: it carries the approved idea, the safe recipe catalog, optional capability awareness, the exact JSON contract, and creative boundaries (do not invent a new idea; never change WHAT the idea is; format/style are data tokens; select a known recipe; no code/paths/URLs/provider details; do not claim unavailable capabilities are implemented). `ICreativeDirector`/`CreativeDirector` validates the idea, builds the prompt, calls the existing `IAiTextGenerator` once with `AiResponseFormat.JsonObject`, parses/validates the untrusted response, and applies a deterministic guardrail that preserves the approved audience and idea reference (no semantic/AI scoring). `CreativeDirectionOptions.AllowBeyondCurrentCapabilities` (default true) lets the director propose an ambitious style while resolution still reports `Ready`/`ReadyWithFallbacks`/`Unsupported` honestly. LLM output is untrusted data: it can never introduce .NET types, assembly names, paths, shell, Python/JS, package installs, model download URLs, provider workflows, or secrets. No auto-registration, no production start, no Qwen call on this machine. Registered as DI (`ICreativePlanningContextProvider` singleton, `ICreativeDirector` scoped); the production pipeline, `SceneVisualRouter`, and Concept/Recipe/Capability behavior are unchanged.

- Concept Registry v1 (no schema/migration, no LLM, no production routing): a new application-layer `AIStudio.Application.Concepts` model separates WHAT to make (concept) from HOW it is produced (recipe) and what is currently possible (capability registry). `ConceptId` (validated lowercase value object, may start with a digit) and `ConceptVersion` (positive int) are data identity; `ConceptIdentifier` validates format/style/tag tokens as plain data so values such as `anime-short` or `anime-cinematic` never require an enum, subtype, handler, or workflow. `ConceptManifest` carries id, version, `Title`, `Description`, `Audience`, `Format`, `Style`, `RecipeId`, optional `RecipeVersion`, `Duration`, and `Tags`, with explicit `[JsonPropertyName]` names for a clean Qwen-friendly JSON shape; it references a trusted recipe and never copies recipe capability requirements. `ConceptValidator` is structural only: invalid id/version, empty title, malformed format/style, invalid recipe id, duration outside `1..86400` s, and invalid/duplicate tags; it never consults the recipe/capability registries and never performs quality, trend, or profitability scoring, so a valid concept referencing an unknown or currently unproducible format stays valid data. `ConceptRegistry` is the trusted in-memory boundary: it validates on `Register`, fails fast on duplicate id+version, orders deterministically by id then version, and supports `Get`/`GetLatest`; concepts are data and cannot introduce executable behavior. `ConceptResolver` maps a concept to its recipe (exact requested version, or the deterministically chosen latest registered version when omitted) via additive `IProductionRecipeRegistry.TryGet`/`TryGetLatest`, then delegates entirely to `IProductionRecipeResolver` — provider priority, fallback ordering, and gap reasons are never duplicated and no provider is executed. `ConceptResolutionStatus` is `Ready` (all required primaries resolve), `ReadyWithFallbacks` (producible, at least one fallback), or `Unsupported` (missing recipe, or a required capability unresolved); `ConceptResolution` exposes the chosen recipe version, the underlying `ProductionRecipeResolution`, inherited `CapabilityGap`s, and issues that keep `recipe_not_found` distinct from `recipe_unsupported`. `SeedConcepts` holds exactly two formats as data: `local-ai-tech-explainer` v1 (youtube-longform/clean-tech → `tech-explainer` v1) and `local-ai-motion-comic` v1 (youtube-short/motion-comic → `motion-comic` latest). Security boundary: concept data may contain only title/description/audience/format/style/duration/tags and a recipe reference — it cannot contain or activate type/assembly names, paths, shell, Python/JS, model download URLs, package installs, provider workflows, scripts, or secrets, and cannot dynamically load types/plugins/providers. Clean JSON uses `[JsonPropertyName]` plus converters for `ConceptId`/`ConceptVersion`/`ProductionRecipeId`/`ProductionRecipeVersion`, so a future AI planner can return a manifest directly; no Qwen integration, prompt, or model call exists yet. Registered as DI singletons; the existing production pipeline, `SceneVisualRouter`, and capability/recipe behavior are unchanged.

- Production Recipe v1 (no schema/migration, no production routing): a new application-layer `AIStudio.Application.ProductionRecipes` model answers "what capabilities does this kind of production require?". `ProductionRecipeId` (lowercase stable value object) and `ProductionRecipeVersion` (positive int, `[JsonConstructor]`) are data, so a future Concept Registry can define a new format without a new C# type, enum, or subclass; `ProductionRecipe` is `Id` + `Version` + `DisplayName` + `IReadOnlyList<ProductionRecipeRequirement>`. `ProductionRecipeRequirement` wraps the capability-registry `CapabilityRequirement` (ordered fallback chain) and adds required/optional; it can only REQUEST capabilities — no provider class, path, script, model location, workflow file, or secret can be expressed. `ProductionRecipeValidator` rejects invalid id/version, empty requirements, no required requirement, duplicate primary capability, a primary repeated in its own fallback, and duplicate fallbacks; a valid but unimplemented capability (e.g. `visual.character_animation`) is still valid recipe data, because validation never consults the registry. `ProductionRecipeRegistry` is in-memory (no DB/Redis/file watcher), fails fast on invalid or duplicate id+version registrations, orders deterministically by id then version, and supports `Get(id, version)`/`GetLatest(id)`/`Contains`. `ProductionRecipeResolver` delegates every requirement to `ICapabilityRegistry.Resolve` (one call per requirement, in order) so provider priority, fallback ordering and gap reasons stay defined once; it executes no provider. `ProductionRecipeResolution` exposes recipe id/version, `IsProducible`, per-requirement `CapabilityResolution` (selected provider, fallback use), and all gaps; `Unsupported` requires at least one unresolved REQUIRED capability, while an unresolved optional capability is reported but non-blocking. `SeedProductionRecipes` holds exactly two formats as data: `tech-explainer` v1 (speech.narration; visual.diagram→visual.ui_motion→visual.still; music.instrumental; media.compose; subtitle.burned) and `motion-comic` v1 (speech.narration; visual.ai_image→visual.still; music.instrumental; media.compose; subtitle.burned). All recipe/value models round-trip through plain `System.Text.Json` (the three value objects carry `[JsonConstructor]`). Registered as DI singletons; the existing production pipeline, `SceneVisualRouter`, and the Capability Registry behavior are unchanged.

- Capability Registry v1 (no schema/migration, no production routing): a new application-layer `AIStudio.Application.Capabilities` model separates CAPABILITY ("what can the studio do") from PROVIDER ("which trusted implementation provides it"). `CapabilityId` is a validated dotted string value object (not an enum) so future content concepts can request known ids as data without recompiling; `CapabilityIds` declares the v1 set. `CapabilityDescriptor`, `CapabilityProviderDescriptor` (id, capability, priority, enabled, runtime category; no executable/model paths), `CapabilityAvailability` (REGISTERED vs ENABLED only — live runtime health is deferred), `CapabilityRequirement` (ordered fallback capability chain), `CapabilityResolution`, and `CapabilityGap` complete the model behind `ICapabilityRegistry`. The pure, deterministic `CapabilityRegistry` fails fast on duplicate capability/provider ids or a provider for an unknown capability, orders providers by priority (descending) then provider-id ordinal, and resolves the preferred enabled provider or reports an explicit gap distinguishing `CapabilityNotImplemented` (no provider registered) from `ProviderDisabled` (registered but disabled), preserving the primary capability's gap reason when fallbacks also fail. `ProductionCapabilityCatalog` maps the current trusted stack with configuration-driven enablement: `speech.narration`→voxcpm2, `music.instrumental`→ace-step, `visual.still`→svg-still, `visual.ui_motion`→animated-svg, `media.compose`→ffmpeg-compose and `subtitle.burned`→ffmpeg-subtitle-burn always enabled; `visual.diagram`→manim, `visual.ai_image`→comfyui-flux, `visual.three_d`→blender follow `Manim:Enabled`/`ComfyUi:Enabled`/`Blender:Enabled`. It is registered as a DI singleton built from options metadata only — it never instantiates or contacts a provider, so resolution works with every optional runtime absent (the current office environment). AI safety: providers are compile-time trusted registrations; the registry cannot load assemblies/plugins, execute scripts or LLM-generated code, download packages/models, resolve class names, or accept executable paths from data. `SceneVisualRouter` and all production handlers are unchanged; a new content concept is normally data, not new application code.

- Narrative Sync + Scene Timing + Visual Polish v1 (no schema/migration): `GenerateAudio` now speaks the approved `ReviewedScript` (`title`/`openingHook`/`sections[]`/`closing`) instead of fabricated title+heading text. `ReviewedNarrationScript` parses the reviewed script and maps it to storyboard scenes deterministically (positional opening+sections+closing, else exact heading match, else structured `audio_narration_source_unmapped`). `NarrationText.Normalize` strips quote marks (VoxCPM2 emits degenerate near-zero-length audio for quoted spans) so speech and subtitles share one normalized source. Narration is generated per scene (`narration/scene_{i}.wav`, fingerprinted on text/voice/locale), assembled by the CPU `FfmpegNarrationAssembler` into `narration.wav` at speech-driven offsets; scene length is `introLead(0.25) + measured narration + outroHold(0.45)` with the 0.35s crossfade overlap, so assembled audio and rendered video share one timeline. Subtitles are derived phrase-by-phrase from the same narration inside each scene's speech window; choreography beats are scheduled inside the narration window; FLUX prompts forbid typography and a trusted SVG overlay supplies labels. `RenderVideo?variant=` yields a review artifact without overwriting canonical outputs. The AudioProduction manifest is v2 and records per-scene narration/timing.

- Visual Direction + Multi-Engine Choreography v1 (no schema/migration): deterministic `SceneVisualDirector` (intent from heading/visual) and `SceneVisualRouter` (preferred engine with documented availability fallback: ThreeD -> Manim -> AnimatedSvg; AiImage -> Manim -> AnimatedSvg; Manim -> AnimatedSvg), a curated `AnimationPrimitive`/`AnimationBeat`/`SceneChoreography` model + `ChoreographyEvaluator`, and a direction-driven `SceneVisualPlanner` (`PlannerVersion=3`). New `SceneVisualEngine.AnimatedSvg` renders a frame-sequence motion-graphics clip (`FfmpegSceneVisualRenderer.RenderAnimationAsync`); FLUX stills get a real motion treatment (`RenderImageMotionAsync`: slow push + animated overlay) instead of a static image. New trusted Manim `process_flow` template covers download/install and model-pull. `SceneAsset.Creator` now records `Engine+per-scene-fingerprint` for narrow per-scene reuse/invalidation; `GenerateSceneVisuals` gained an explicit `force` flag for intentional regeneration while manual assets are otherwise preserved. `RenderVideo` gained an optional `variant` query (e.g. `?variant=motion-v1`) so a review render never overwrites the canonical MP4. Blender seed clamped to Cycles' int32 range (root-cause fix).

- Production Orchestration v2 (no schema/migration): new durable `JobType.GenerateAudio` (`GenerateAudioWorkflow`, `GenerateAudioJobHandler`, `GenerateAudioJobPayload/Result`, new `POST /api/content-projects/{id}/storyboard-jobs/{storyboardJobId}/audio-jobs`). One job runs three internally staged steps — narration (VoxCPM2 Formal/id-ID, text derived deterministically from the storyboard title + scene headings), background music (ACE-Step instrumental, 90 BPM, seed 42, duration = ceil(narration)+2s clamped 5–120s) and the final mastered mix (`IAudioMixer`, 48 kHz stereo). Durable reuse: `AudioProductionWorkspace` stores artifacts at deterministic `audio/{projectId}/{storyboardJobId}/{narration,music,master}.wav` plus a small `manifest.json` of per-stage input fingerprints; each stage persists right after it completes, so a retry reuses every stage whose fingerprint, SHA-256 and probed media still match and regenerates only missing/invalidated stages. `RenderVideoResult` gained `audioSource`/`masteredAudioPath`/`masteredAudioContentHash` (narration track now optional); `RenderVideoJobHandler`/`RenderVideoWorkflow` prefer the validated master and fall back to the legacy narration track when absent. GPU semantics unchanged: providers own their `IGpuResourceGate` lease, orchestration holds none, the mixer stays CPU-only.

- Audio Generation v1 (providers only, no schema/migration, both engines disabled by default): new application boundaries `ISpeechSynthesisProvider` (`Application.Rendering.AudioGeneration`) and `IMusicGenerationProvider`. `VoxCpmSpeechSynthesisProvider` maps a small curated voice set (Formal/Playful/Energetic/Documentary) to approved Indonesian control text and launches the repository-owned `Rendering/VoxCPM2/synthesize.py` with a trusted model path plus one constrained JSON request (text, voice, timesteps, cfg, normalize, output). `AceStepMusicGenerationProvider` launches `Rendering/ACE-Step/generate.py` with a trusted project root/model plus one constrained JSON request (caption, duration, bpm, instrumental, seed, steps). Both persist the generated WAV through `IAssetFileStore`, verify it with `IMediaInspector` (audio present, duration > 0, expected sample rate; 2 channels and duration match for music), acquire the shared `IGpuResourceGate` for the process only and release before the CPU-only persist/probe, and map stable `speech_*` / `music_*` errors. Callers cannot supply python code, executable/model/project paths, CLI flags, or shell fragments. Config: `SpeechSynthesis:VoxCPM2:*` and `MusicGeneration:AceStep:*`; no `/home/tama` paths in source.

- Audio Mixing / Mastering v1 (no schema/migration): new standalone `IAudioMixer`/`FfmpegAudioMixer` (`Application.Rendering.AudioMixing` + `Infrastructure.Rendering.AudioMixing`). Narration + optional background music -> narration `loudnorm=I=-16:TP=-1.5:LRA=11`, music `loudnorm=I=-24:TP=-3:LRA=7`, sidechain ducking while narration is active, short music fade-in/out, `alimiter` at -1 dBFS, 48 kHz stereo `pcm_s16le` WAV bounded to the narration length (music looped when shorter, trimmed/faded when longer). Values centralized in `AudioMixingOptions`; no caller-supplied filter text. Inputs are approved asset references resolved through existing `IAssetFileStore`; output is a relative `.wav` under the approved root; execution reuses `IProcessRunner`/`IMediaInspector`. Structured `audio_*` errors. CPU-only: never acquires `IGpuResourceGate`; no shell; no arbitrary executable paths.

- Video Quality v1.1 (renderer tuning + first Visual Asset Engine, no schema/migration):
  - Subtitle: `SubtitleStyle` defaults lowered from FontSize 22 / Outline 2 / MarginV 40 to FontSize 16 / Outline 1 / MarginV 18. Note libass renders SRT through a 288-high script canvas (≈2.5x scale at 720p), so MarginV 18 is a ~45px safe bottom margin; the earlier 40 sat ~100px up into the visuals.
  - Motion: default cycle is now centered zoom only (`slow_zoom_in` / `slow_zoom_out`, 8% travel); `pan_left` / `pan_right` are opt-in via `SceneMediaInput.Motion` and only used for visuals with declared safe margins. No computer vision.
  - Transition: default crossfade reduced 0.5s -> 0.35s; `cut`/`fade`/`crossfade` unchanged.
  - Music/ducking: background music now gets `highpass=f=120` and default volume 0.28; ducking ratio 8 -> 4. Narration is gain-staged with `loudnorm=I=-16:TP=-1.5:LRA=11,aresample=48000,asetpts=PTS-STARTPTS` **only when the render mixes audio**, so a quiet narration is audible and reliably triggers ducking. `asetpts` fixes a loudnorm timestamp offset that truncated the mix under `amix duration=first`; `aresample` avoids loudnorm's 192kHz output. A narration-only render is byte-for-byte unaffected.
  - Visual Asset Engine: `SceneVisualSvg` (pure, Application) composes a structured `SceneVisualBrief` into 1280x720 SVG (Hero/Cards/Window/Chat layouts, controlled palettes, built-in icons); `FfmpegSceneVisualRenderer` (Infrastructure) rasterizes it to PNG through the existing `IProcessRunner`/FFmpeg `librsvg` decoder. No new runtime, model, or NuGet dependency. Visuals reserve the bottom ~90px for subtitles.
- Demo artifact: `src/AIStudio.Api/assets/renders/vq1-demo/quality-v1.1.mp4` (2113072 bytes, 24s, 1280x720, h264/aac 48kHz stereo, styled subtitles burned in, SHA-256 `a7821689f38f039c6197b6c1bde2328395c5734fd34c0af1ab8b386f547402cc`), rendered by the real `FfmpegVideoRenderer` against the canonical Video #1 narration/subtitle and 8 composed visuals (`assets/vq1-demo/visuals/`), with an original locally synthesized music bed (`assets/vq1-demo/music_v1_1.wav`). Canonical Video #1 and `quality-v1.mp4` are untouched.
- Video Quality v1.2 (transition ghosting fix, no schema/migration): optional `SceneMediaInput.VisualKind` (`SceneVisualKind.Photographic` default, `Graphic` for text-heavy/UI/infographic) makes `FfmpegCommandPlan` choose the crossfade type per boundary. A boundary touching a `Graphic` scene uses `xfade transition=fadeblack` (fade through background) instead of `fade`, eliminating double-text ghosting; photographic boundaries keep `fade`. `cut`/`fade` and the 0.35s overlap (hence scene timing/offsets) are unchanged. Classification is deterministic and explicit; no computer vision or new dependency.
- Demo artifact: `src/AIStudio.Api/assets/renders/vq1-demo/quality-v1.2.mp4` (2012269 bytes, 24s, 1280x720, h264/aac 48kHz stereo, styled subtitles burned in, SHA-256 `4babcdf33f592c9a82fc60f55af12c0118fd4f50315cf4ae3f5d8952856b79f8`), rendered by the real `FfmpegVideoRenderer` against the v1.1 composed visuals marked `Graphic` so every boundary fades through background. `quality-v1.1.mp4` and canonical Video #1 are untouched.
- Durable Visual Asset Pipeline (Phase 3, no schema/migration): new durable `JobType.GenerateSceneVisuals`. `GenerateSceneVisualsWorkflow` gates on a completed GenerateStoryboard result; `GenerateSceneVisualsJobHandler` plans each scene, renders it, persists bytes under the approved root via the extended `IAssetFileStore.WriteAsync` (path-safe, hash-verified, idempotent), and registers a canonical `SceneAsset` (`Origin=Local`, `Source=ai-studio:scene-visual-engine`). Existing canonical/manual assets are skipped, never overwritten, so re-runs are idempotent. `RenderVideoJobHandler` now sets `SceneMediaInput.VisualKind` from asset provenance (`Graphic` for generated visuals, `Photographic` for manual). Enqueue: `POST /api/content-projects/{contentProjectId}/storyboard-jobs/{storyboardJobId}/visual-jobs`.
- Visual planning / engine selection (Phase 3/4): `SceneVisualPlanner` deterministically maps the existing `heading`+`visual` into a `SceneVisualPlan` (Hero/Cards/Window/Chat layout, palette cycle, cards/icons, window command/progress, optional animation template). `SceneVisualEngineSelector` maps template -> `SceneVisualEngine` (`SvgStill` / `ManimAnimation`). No model call and no durable plan schema.
- Manim animation engine (Phase 4): `IManimSceneRenderer`/`ProcessManimSceneRenderer` run the repository-owned `render_scene.py` + `templates.py` through `IProcessRunner` structured arguments (no shell). Only allowlisted template names and text/palette/duration data cross the boundary; arbitrary model-generated code is never executed. Templates: `local_ai_flow`, `chat_flow`. `Manim:Enabled=false` by default, so the planner falls back to `SvgStill`. Manim Community 0.20.1 is installed locally in `ai-studio/.venv` (conda-forge, gitignored; Text/shapes only, no LaTeX).
- Animated demo: `src/AIStudio.Api/assets/renders/vq2-demo/animated-explainer-v1.mp4` (1585886 bytes, 24s, 1280x720, h264/aac 48kHz stereo, styled subtitles burned in, SHA-256 `fa53b42f3c94c2537fbe691b29f842d4c587659488a8249e80d5ad4f7460e6b8`), built from the real Video #1 storyboard via `SceneVisualPlanner`, the real `FfmpegSceneVisualRenderer` and `ProcessManimSceneRenderer`, and `FfmpegVideoRenderer`: 2 Manim scenes (`LocalAiFlow`, `ChatFlow`) + 6 composed SVG stills. Previous demos and canonical Video #1 are untouched.
- Manim/SVG design consistency (v2): Manim templates receive the shared palette as structured `colors` (single source `SceneVisualSvg.GetColors`), use DejaVu Sans, and reuse the SVG kicker/headline/accent-underline header, accent chips, layered dark background, and reserved bottom ~90px subtitle zone. `local_ai_flow` and `chat_flow` were reworked for larger objects and short labels only (LOCAL/PRIVATE/OFFLINE; HALO/SIAP).
- Narration-aware timing (v2): `SceneTiming` allocates narration by scene text weight with a 2.0s static / 3.5s animated floor, always summing to the narration duration. `SceneMediaInput.Weight` carries the weight; `FfmpegCommandPlan` derives per-scene durations and cumulative crossfade offsets.
- Durable timing parity (SceneTiming single source of truth): `SceneTiming.AllocateForScenes` (and `FfmpegCommandPlan.ResolveContentDurations`) is shared by the demo harness, durable `GenerateSceneVisuals` (Manim clip duration) and durable `RenderVideo` (scene content durations), so no second timing implementation exists. `GenerateSceneVisualsJobHandler` reads the project narration via `INarrationRepository` + `IMediaInspector` and falls back to the deterministic animation floor when narration is absent or unreadable.
- SubtitleTimeline (narration-aware subtitles): `SubtitleTimeline` serializes cue boundaries from the shared scene timing and reads canonical SRT cue text. Durable `RenderVideoJobHandler` passes the canonical cue text (`VideoRenderRequest.SubtitleCueTexts`) only when cue count equals scene count; `FfmpegVideoRenderer` burns a transient derived SRT and leaves the canonical subtitle asset untouched. Other cue shapes keep canonical timing as the deterministic fallback.
- SceneVisualPlanner v2 (SVG regression fix): display text is resolved only from the canonical heading plus explicit quoted on-screen strings and a small generic concept vocabulary. Layout classification uses heading intent plus structural cues (a real `ollama <verb>` command, >=2 model options); command extraction requires a real command. Raw `visual` instruction prose is never truncated into cards/hero/bubbles, and Manim `secondary`/`tertiary` surface quoted chat dialogue only.
- Animated demo v2: `src/AIStudio.Api/assets/renders/vq2-demo/animated-explainer-v2.mp4` (1551000 bytes, 24s, 1280x720, h264/aac 48kHz stereo, styled subtitles burned in, SHA-256 `b74e37b1ffcce24c46df579ca92f633aa1b91354273f175f742533a68de9bae9`); per-scene durations 3.5 / 3.5 / 2.979 / 3.011 / 2.760 / 3.117 / 2.655 / 2.477s. v1 and all earlier demos are untouched.
- Animated demo v3 (local, gitignored): `src/AIStudio.Api/assets/renders/vq2-demo/animated-explainer-v3.mp4` (1588804 bytes, 24s, 1280x720, h264/aac 48kHz stereo, styled subtitles burned in, SHA-256 `966d60ef97e640e18711929389c6076fc2f0e009d8490f33fefedd910dc36225`). Scenes 0–1 Manim clips are byte-identical to v2; scenes 2–7 use the fixed SceneVisualPlanner v2 structured layouts (requirements grid, install terminal, model cards + pull command, chat UI, tips cards, closing hero) with no raw instruction text. v2 and all earlier demos are untouched.
- Video Quality v1 (renderer quality pass, no schema/migration): deterministic scene motion (`none`, `slow_zoom_in`, `slow_zoom_out`, `pan_left`, `pan_right` via `zoompan`), controlled transitions (`cut` concat, `fade` per-scene through black, `crossfade` via `xfade` with overlap-compensated scene duration), libass-styled burned-in subtitles (`subtitles` filter + `force_style`), and optional config-driven background music with `sidechaincompress` ducking plus renderer-supported per-scene SFX (`adelay` + `amix`).
- Subtitle decision: styled subtitles are burned in and no soft `mov_text` stream is embedded, because FFmpeg's MP4 muxer forces a lone subtitle track to `default=1` and would double-render on top of the burn-in. `RenderVideoResult.SubtitleBurnedIn` records this and `FinalVideoQaJobHandler` skips the soft-stream requirement only for burned-in subtitles; older results default to `false` and keep the previous behavior.
- Demo artifact: `src/AIStudio.Api/assets/renders/vq1-demo/quality-v1.mp4` (1077280 bytes, 24s, 1280x720, h264/aac, no soft subtitle stream, SHA-256 `cbe6958593212fb024bf6239998f920f44a41558b54f6213f9df25b0a584fc9e`), rendered by the real `FfmpegVideoRenderer` against the canonical Video #1 scene/narration/subtitle assets and an original locally synthesized music bed (`src/AIStudio.Api/assets/vq1-demo/ambient_test.wav`). The canonical Video #1 MP4 was not touched.
- Video #1 E2E run: ContentProject `01b7f0ef-e749-4e1d-bbad-a93d016b2be2`; GenerateIdea -> GenerateScript -> approved ReviewedScript -> GenerateStoryboard (8 scenes) -> 8 scene assets -> narration -> subtitle -> RenderVideo -> FinalVideoQa.
- Project AI stayed local (Ollama `qwen3.8:27b-q4_K_M`); no paid or external provider was used.
- Config correction: `Ollama:TimeoutSeconds` raised 120 -> 600. The validated model generates at roughly 9 tokens/second, so the GenerateScript 3000-token budget exceeded the old 120s client timeout and failed with `ai_timeout` across all retries; the existing `1..600` validation bound already anticipated this.
- Routine reasoning off: the typed `AiTextRequest` now carries an optional `Think` flag that maps to the Ollama `/api/chat` `think` field. `GenerateScriptJobHandler` requests `Think: false`; callers that leave it null keep the previous thinking behavior. A fresh GenerateScript job (`012aaac4-fdcf-4b2f-b3a6-82f4d42e7c60`) completed with `retryCount` 0 in ~76s / 648 output tokens; the earlier failed job (`196926e0-f9a1-48f7-a360-ac52ec320945`) is preserved untouched.
- Artifact: `src/AIStudio.Api/assets/renders/01b7f0efe7494e1dbbada93d016b2be2/1526be4b6d434f54999782cc368a1990.mp4` (499638 bytes, 24s, 1280x720, h264/aac/mov_text, SHA-256 `2a1e6980da36edadbbed375bbde8fd802669892b1ca2ecfcb726ef74b7884fb8`).
- Untracked local media under `src/AIStudio.Api/assets/` was intentionally preserved for manual inspection and is not committed.
- AI Image Engine (ComfyUI, optional, no schema/migration): new `IImageGenerationProvider` boundary (`ImageGenerationRequest` = prompt + stable seed) with `ComfyUiImageGenerationProvider` in Infrastructure. It submits one repository-owned approved ComfyUI API workflow (`src/AIStudio.Infrastructure/Rendering/ComfyUI/flux2_klein_4b_distilled.json`, FLUX.2 Klein 4B Distilled + `qwen_3_4b_fp4_flux2` + `flux2-vae`) and injects only prompt/dimensions/seed (`JsonEncodedText`-escaped; no graph is built from model output). `SceneVisualEngine` gained `AiImage`; `SceneVisualEngineSelector`/`SceneVisualPlanner` choose it only for still scenes when the provider is enabled, so animation still wins and `ComfyUi:Enabled=false` (default) preserves SVG behavior. Errors map to stable `image_*` `RenderVideoException` codes surfaced as `JobExecutionException`; timeout is enforced by a linked CTS. `GenerateSceneVisualsJobHandler` reuses the existing `SceneAsset`/`IAssetFileStore` persistence unchanged. `SceneImagePrompt` composes a deterministic no-text prompt from the existing brief.
- AI image provenance note: generated AI stills are registered with `Source=ai-studio:scene-visual-engine`, so `RenderVideoJobHandler` currently treats them as `Graphic` (fade-through-background). A photographic `VisualKind` refinement is deferred.
- ThreeD Engine (Blender, optional, no schema/migration): new `IThreeDRenderingProvider` boundary (`ThreeDRenderRequest` = template + palette + duration + stable seed) and `BlenderThreeDRenderingProvider` in Infrastructure. It renders one repository-owned Blender template (`Rendering/Blender/templates/local_ai_laptop.py`) to a PNG image sequence via `IProcessRunner` (`blender -b --factory-startup --python ... -- --params ... --frames ...`, `ArgumentList`, no shell) and encodes it to H.264/yuv420p with the shared FFmpeg `IProcessRunner` boundary. `SceneVisualEngine` gained `ThreeD`; selection is narrow and explicit — at most one scene whose text matches both a laptop keyword and a local/offline AI signal, only when no Manim template already claimed the scene, and only when `Blender:Enabled=true` (default false). Only structured data (palette, duration, seed) and configuration-owned render settings cross into Python; no caller source/paths/graphs. Errors map to stable `threed_*` codes; each process is bounded by configuration timeout. `GenerateSceneVisuals` reuses existing `SceneAsset`/`IAssetFileStore` persistence (`AssetType.Video`, `Creator=ThreeD`). CUDA is the verified backend; OptiX is deferred and non-blocking.
- GPU Resource Gate v1 (process-local, no schema/migration): new `IGpuResourceGate` application boundary returning an async-disposable lease, implemented by `GpuResourceGate` (`SemaphoreSlim(1,1)`, `GpuResourceGateOptions.Enabled=true` by default) and registered as a **singleton** so every heavy-GPU provider shares one permit. Leases are idempotent (double dispose releases once), release deterministically via `await using`, and waiters honor cancellation without consuming the permit. Consumers: ComfyUI holds the lease across the full submit -> poll -> download window (POST /prompt only queues, so releasing after submit would be wrong); Blender holds it only across the CUDA PNG render and releases before the CPU-only FFmpeg encode; Ollama holds it across the `api/chat` generation call (its provider boundary was clean, so it is integrated). Waiting is logged (workload, wait ms, acquired, released); no prompts/content logged. The gate is process-local only — it does not coordinate other AI Studio processes, manually launched ComfyUI/Blender jobs, external Ollama clients, or Windows GPU work. No priorities, fairness, VRAM awareness, or cross-process locking.
- Provenance: Blender clips carry the same generated-visual source marker, so `RenderVideoJobHandler` treats them as `Graphic`; `Creator=ThreeD` distinguishes them from SVG/Manim/ComfyUI.

## Verification

- PASS - Qwen Story Bible Planner v1 focused tests (2026-09-21): Bibles 134 pass (+22 `QwenStoryBiblePlannerTests`: valid proposal; recurring character/world id reused across beats; invalid JSON, additional properties, malformed character/world ids rejected; duplicate character/world ids rejected; unknown/duplicate beat grounding rejected; grounding refs not in the proposal rejected; relationship to unknown character rejected; preschool fixture on the same generic contracts; one request per build; cancellation token flows to `IAiTextGenerator`; no registration/plan-mutation side effects and an `IAiTextGenerator`-only constructor; stable-identity-vs-state, exact-id-reuse, plan-authority, and no-leak prompt rules; proposal→register→`StoryPlanGrounder`→`StoryContextBuilder` proven), StoryContext 50, Stories 99, Architecture 3, all 0 failed. `dotnet build AIStudio.slnx -c Release` 0 warnings/0 errors; `dotnet format AIStudio.slnx --verify-no-changes` clean; `git diff --check` clean. No real Qwen/media/DB/Asset Registry work; Script/Storyboard/StoryPlanGrounder unchanged.

- PASS - Character + World Bible Grounding Foundation v1 focused tests (2026-09-21): Bibles 112 pass (+16 `StoryPlanGroundingTests`: shared character/world id reused across beats; narrative fields and beat order/role/purpose/duration/continuity preserved; partial grounding keeps untouched refs; explicit grounding replaces only refs; original plan never mutated; unknown beat / duplicate assignment / unknown character / unknown world / malformed id rejected with stable codes; explicit empty refs allowed; empty-ref plan valid with no assignments; StoryContext resolves grounded refs and excludes unrelated bibles; beat-scoped StoryContext excludes unreachable refs; CharacterState/WorldState do not mutate identity; anime and preschool fixtures use the same mechanism; grounding contracts carry no runtime detail), StoryContext 50, Stories 99, Architecture 3, all 0 failed. `dotnet build AIStudio.slnx -c Release` 0 warnings/0 errors; `dotnet format AIStudio.slnx --verify-no-changes` clean; `git diff --check` clean. No real Qwen/media/DB/Asset Registry work; Script/Storyboard untouched; visual consistency not claimed.

- PASS - Real-Qwen Storyboard validation harness (2026-09-21): harness self-check 12/12 deterministic checks (valid result recognized; malformed JSON, missing scene field, and empty scene collection rejected via the production deserializer; provider/model and filesystem/command leakage detected; legitimate camera/framing language allowed; rewritten spoken dialogue detected while in-world displayed text is allowed; artifact serialization; PASS/REVIEW/FAIL mapping; scene-count differences do not fail). Jobs 85, StoryContext 50, all 0 failed. `dotnet build AIStudio.slnx -c Release` 0 warnings/0 errors; `dotnet format AIStudio.slnx --verify-no-changes` and the harness project format clean; `git diff --check` clean; `bash -n scripts/e2e-storyboard-qwen.sh` clean. No real Qwen call; no production change; no DB/media/Asset Registry.

- PASS - Storyboard Context Integration v1 focused tests (2026-09-21): Jobs 85 pass (new `GenerateStoryboardStoryContextTests` 8: compact context projected into the prompt with ordered beats and the section-per-beat mapping; authoritative direction/plan passed to the builder; only referenced characters/worlds included and unrelated bible entries excluded; empty refs tolerated; optional state projected without mutating identity; visual/camera language allowed while provider/path/command detail and Script rewriting are forbidden; no-context behavior unchanged), StoryContext 50, Bibles 96, Stories 99, all 0 failed. `dotnet build AIStudio.slnx -c Release` 0 warnings/0 errors; `dotnet format AIStudio.slnx --verify-no-changes` clean; `git diff --check` clean. No real Qwen call; no DB/job/API/media/Asset Registry change; Character/World grounding not end-to-end validated.

- PASS - Script Spoken-Content Boundary Refinement v2 focused tests (2026-09-21): Jobs 77 pass (+1 `GenerateScriptSpokenContractTests` asserting cue-translation wording; existing spoken-contract and camera/storyboard assertions updated), harness self-check 14/14 unchanged. `dotnet build AIStudio.slnx -c Release` 0 warnings/0 errors; `dotnet format AIStudio.slnx --verify-no-changes` clean; `git diff --check` clean; `bash -n scripts/e2e-script-qwen.sh` clean. No real Qwen call; no schema/StoryContext/StoryPlan/Creative/Story Director change; harness boundary/leak/concept-audience logic unchanged.

- PASS - Script Spoken-First refinement focused tests (2026-09-21): Jobs 76 pass (+2 `GenerateScriptSpokenContractTests` asserting the prompt defines openingHook/narration/closing as spoken words and forbids production direction, with and without StoryContext), harness self-check 14/14 (concept/audience low overlap stays REVIEW; boundary hits review/FAIL; sound/visual-effect detected; ordinary narration and quoted dialogue allowed). `dotnet build AIStudio.slnx -c Release` 0 warnings/0 errors; `dotnet format AIStudio.slnx --verify-no-changes` and the harness project format clean; `git diff --check` clean; `bash -n scripts/e2e-script-qwen.sh` clean. No real Qwen call; no schema/StoryContext/StoryPlan/Creative/Story Director change; Character/World grounding still not exercised.

- PASS - Real-Qwen Script validation harness (2026-09-21): harness self-check 12/12 deterministic checks (valid script recognized; malformed JSON and missing-field rejected via the production deserializer; section/beat mismatch detected; camera/shot, provider/path, and non-spoken production instructions detected; ordinary spoken narration and quoted dialogue allowed; concept-overlap signal; artifact serialization; PASS/REVIEW/FAIL mapping). Jobs 74, StoryContext 50, Stories 99, Bibles 96, all 0 failed. `dotnet build AIStudio.slnx -c Release` 0 warnings/0 errors; `dotnet format AIStudio.slnx --verify-no-changes` and the harness project format clean; `git diff --check` clean; `bash -n scripts/e2e-script-qwen.sh` clean. No real Qwen/model call was made; Creative/Story Directors were not rerun; no media/DB/job/API/Storyboard change.

- PASS - Script Context Integration v1 focused tests (2026-09-21): Jobs 74 pass (new `GenerateScriptStoryContextTests` 5: compact context projected into the prompt with ordered beats and concept/audience; only referenced characters/worlds included and unrelated bible entries excluded; optional state projected without mutating identity; authoritative direction/plan passed to the builder; empty character/world refs tolerated; no-context behavior unchanged), StoryContext 50, Stories 99, Bibles 96, all 0 failed. `dotnet build AIStudio.slnx -c Release` 0 warnings/0 errors; `dotnet format AIStudio.slnx --verify-no-changes` clean; `git diff --check` clean. No real Qwen/model call; no Storyboard, Asset Registry, DB, job, or API expansion.

- PASS - Story Director Boundary Refinement v1 (2026-09-21): prompt-only change to `QwenStoryDirectorPrompt`; Stories 99 pass (was 96; +3 focused prompt tests: creative-direction visuals as context/translation, forbidden cinematography/editing execution terms, allowed narrative visual state). `dotnet build AIStudio.slnx -c Release` 0 warnings/0 errors; `dotnet format AIStudio.slnx --verify-no-changes` clean; `git diff --check` clean. No real Qwen call was made; parser, validator, `StoryPlan`, `NarrativePattern` registry, DI/mode selection, deterministic director, and the validation harness heuristic were untouched.

- PASS - Real-Qwen Story Director validation harness (2026-09-21): harness self-check 11/11 deterministic checks (valid plan recognized; malformed/invalid-duration/continuity-cycle rejected via the production parser; script-dialogue and provider/url leak heuristics; clean-prose non-trigger; CreativeDirection artifact round-trip; artifact serialization; PASS/REVIEW/FAIL mapping); Stories 96 pass (production Story behavior unchanged); `dotnet build AIStudio.slnx -c Release` 0 warnings/0 errors; `dotnet format AIStudio.slnx --verify-no-changes` and the harness project format clean; `git diff --check` clean. No real Qwen call was made, Creative Director was not rerun, no media/DB/job/API/model download.

- PASS - Qwen-backed Story Director v1 focused tests (2026-09-21): Stories 96 pass (existing deterministic `StoryDirector` tests preserved; new `QwenStoryDirector` 12, `QwenStoryDirectorPrompt` 8, `StoryDirectorModeDependencyInjection` 4 covering the AI request/JSON mode, valid plan parse, malformed/additional-property/invalid-identifier rejection, validator failure, source-concept/swapped-pattern/unknown-pattern rejection, provider-failure no-fallback, prompt pattern/contract/boundary/identifier/no-leak/determinism, DI deterministic default + qwen + invalid mode). Also Architecture 3, Creative 38, StoryContext 50, Bibles 96, all 0 failed. `dotnet build AIStudio.slnx -c Release` 0 warnings/0 errors; `dotnet format AIStudio.slnx --verify-no-changes` clean; `git diff --check` clean. No real Qwen call or model generation was made; no validation harness was added; no DB/job/API/media change.

- PASS - Creative Director Real-Qwen contract fix (2026-09-21): all 5 real-Qwen responses were syntactically valid JSON with exactly the expected top-level/concept/treatment keys, but emitted `concept.recipeId` as `<id> v<n>` (e.g. `tech-explainer v1`); the strict `ProductionRecipeId` converter rejects the space, and the wrapped `JsonException` made `CreativeDirectionParser` report `creative_direction_invalid_json`. Prompt-only fix: the recipe catalog now renders `recipeId=<id> recipeVersion=<n>` and the rules require copying the exact id token (no version suffix, no spaces) and forbid extra properties, wrapper objects, markdown fences, and commentary. Parser strictness unchanged (no repair, no field stripping). Creative tests 38 pass (+4 reproducing the failure mode: prompt contract, exact-recipeId-token rule, version-suffixed recipeId rejected, additional properties rejected), Concepts 52, ProductionRecipes 48; Release build 0 warnings; `dotnet format AIStudio.slnx --verify-no-changes` and `git diff --check` clean. Real multi-case Qwen validation NOT rerun by the agent (USER-RUN); no model/API call was made.

- PASS - Creative Director Real-Qwen validation harness cheap checks (2026-09-21): `dotnet build AIStudio.slnx -c Release` 0 warnings/0 errors; `dotnet format AIStudio.slnx --verify-no-changes` and the harness project format clean; harness `--self-check` 11/11 deterministic checks; 170/170 Creative (34) + Concepts (52) + ProductionRecipes (48) + Capabilities (36) tests pass; `git diff --check` clean. Preflight only confirmed Ollama `127.0.0.1:11434` reachable with `qwen3.8:27b-q4_K_M` installed. REAL QWEN MULTI-CASE VALIDATION NOT RUN by the agent (USER-RUN); no generation, model download, media, or migration was performed.
- PASS (office code-only) - Extractable Domain Modules + Reusable Capability Rules: Release build `dotnet build AIStudio.slnx -c Release` 0 warnings/0 errors; `dotnet format AIStudio.slnx --verify-no-changes` clean; `git diff --check` clean. New `AIStudio.Tests.Architecture.ArchitectureBoundaryTests` 3 pass (Domain does not reference Application/Infrastructure/Api; Application does not reference Infrastructure/Api; Infrastructure does not reference Api) using reflection only, no new package. Documentation-only milestone: no source behavior, runtime, package, schema, durable-job, or service-boundary change; no Postgres/AI/media runtime was started or invoked. Frozen PRD source and its SHA-256 are unchanged; the addendum is indexed separately.
- PASS (office code-only) - Story Context Builder v1: Release build `dotnet build AIStudio.slnx -c Release` 0 warnings/0 errors; `dotnet format AIStudio.slnx --verify-no-changes` clean; `git diff --check` clean. StoryContext focused tests 50 pass (full context build, concept/treatment/story projection and compactness, beat purpose/order/duration preservation, deterministic ordering and repeat-build equality, only-referenced character/world inclusion, irrelevant-relationship exclusion, 10-characters/8-worlds -> 2/1 token-efficiency proof, unresolved character/world reported and omitted, latest-Bible-version resolution, asset references excluded by default and safe id/purpose/variant metadata when opted in, beat-scoped context = selected beat + continuity closure with unrelated beats/characters/worlds excluded and unknown-beat issue, optional state projected separately from identity, source-model and registry immutability, JSON round-trip and no provider/path/registry internals, tech/anime/preschool-3D generality on one builder, security reflection, DI wiring); regression suites Bibles 96, Stories 72, Creative 34, Concepts 52, ProductionRecipes 48, Capabilities 36, all 0 failed. Full suite 876 total: 875 pass / 1 skipped (gated real audio E2E) / 0 failed excluding the `AIStudio.Tests.Persistence` namespace (DB-gated). No Ollama/Qwen/ComfyUI/Blender/VoxCPM/ACE-Step/Manim/FFmpeg media runtime was invoked; no asset files were created and no database was started.
- PASS (office code-only) - Character + World Bible v1: Release build `dotnet build AIStudio.slnx -c Release` 0 warnings/0 errors; `dotnet format AIStudio.slnx --verify-no-changes` clean; `git diff --check` clean. Bible focused tests 96 pass (value-object id/version validation, character/world registry register/list/get/latest/duplicate/invalid/missing, structural character and world validation incl. identity tokens, free-text traits, variants, baseline variant, relationships/self-reference, recurring props, continuity rules, locations, asset references, state tokens and approved-variant enforcement, JSON round-trip and clean scalars, strict parser accept/reject incl. unknown executable members, identity-vs-state separation, approved-variant-without-new-character, additive StoryPlan reference validation with unknown detection/dedup, anime/3D-preschool/robot characters and cyberpunk/playroom/office worlds as data without new C#, engine/provider-free reflection, DI wiring); regression suites Stories 72, Creative 34, Concepts 52, ProductionRecipes 48, Capabilities 36, all 0 failed. Full suite 826 total: 825 pass / 1 skipped (gated real audio E2E) / 0 failed excluding the `AIStudio.Tests.Persistence` namespace (DB-gated). No Ollama/Qwen/ComfyUI/Blender/VoxCPM/ACE-Step/Manim/FFmpeg media runtime was invoked; no asset files were created and no database was started.
- PASS (office code-only) - Story Director Foundation v1: Release build `dotnet build AIStudio.slnx -c Release` 0 warnings/0 errors; `dotnet format AIStudio.slnx --verify-no-changes` clean; `git diff --check` clean. Stories focused tests 72 pass (value-object validation, pattern registry register/list/get/latest/invalid/duplicate, structural pattern and plan validation incl. beat id/order uniqueness, duration tolerance, reference tokens, continuity unknown/self/cycle, required-slot coverage, JSON round-trip and clean scalars, strict parser accept/reject incl. unknown executable members, deterministic director plan/continuity/version resolution/no-AI dependency, anime-horror and kids-song patterns as data without new C#, DI wiring); regression suites Creative 34, Concepts 52, ProductionRecipes 48, Capabilities 36, all 0 failed. Full suite 730 total: 729 pass / 1 skipped (gated real audio E2E) / 0 failed excluding the `AIStudio.Tests.Persistence` namespace (DB-gated). No Ollama/Qwen/ComfyUI/Blender/VoxCPM/ACE-Step/Manim/media runtime was invoked.
- PASS - Creative Director v1 focused tests: 34 pass (approved-idea structural validation, GenerateIdea-result mapping, prompt contains approved topic/angle/audience/objective and the no-new-idea rule, safe recipe catalog included, provider implementation details excluded, deterministic prompt, valid JSON parse into `CreativeDirection`, `ConceptValidator` reuse, treatment validation, data-driven `anime-cinematic`/`retro-game-documentary` names, malformed JSON and missing concept/treatment/fields rejection, unknown recipe representable, fake AI called once, approved-audience/reference guardrails, concept resolves through the existing ConceptResolver, descriptor-only resolution, no provider execution, critical regression that the approved idea stays authoritative, DI wiring), 0 failed. Run on the office (code-only) machine with no model call; resolution is metadata-only.
- PASS - Concept Registry v1 focused tests: 52 pass (concept id/version validation, format/style data tokens, structural validation incl. malformed format/style, duration bounds, invalid/duplicate tags, unknown future recipe stays valid, registry register/list/get/latest, duplicate/invalid rejection, clean JSON round-trip with omitted recipe version, known-recipe `Ready`, fallback `ReadyWithFallbacks`, capability-gap `Unsupported`, missing-recipe `recipe_not_found`, latest-recipe resolution, optional-capability inheritance, delegation to `IProductionRecipeResolver`, descriptor-only resolution, new anime-format concept with no new C#, seed catalog shape, DI wiring), 0 failed. Run on the office (code-only) machine with no external runtime; resolution is metadata-only.
- PASS - Production Recipe v1 focused tests: 48 pass (recipe id/version validation, structural recipe validation including empty/duplicate/self-fallback/duplicate-fallback/unimplemented-capability-as-data, registry list/get/latest/duplicate/invalid rejection, deterministic resolution, fully-supported, supported-with-fallback, unsupported, required-vs-optional, provider-disabled and missing-capability gaps, delegation to `ICapabilityRegistry` once per requirement with deterministic provider inheritance, descriptor-only resolution, plain System.Text.Json round-trip, seed catalog shape, DI wiring), 0 failed. Run on the office (code-only) machine with no external runtime; resolution is metadata-only.
- PASS - Capability Registry v1 focused tests: 36 pass (stable ids/validation, registry listing, deterministic resolution, disabled provider gap, missing capability gap, fallback selection, primary-wins, fallback gap reason, provider ordering, duplicate/unknown registration validation, catalog mapping, DI-enablement reflection), 0 failed. Run on the office (code-only) machine with no external runtime; resolution is metadata-only.
- PASS (office code-only) - Creative Director v1: Release build `dotnet build AIStudio.slnx -c Release` 0 warnings/0 errors; `dotnet format AIStudio.slnx --verify-no-changes` clean; `git diff --check` clean. Full suite 658 total: 657 pass / 1 skipped (gated real audio E2E) / 0 failed excluding the `AIStudio.Tests.Persistence` namespace. Earlier Concept Registry v1 run: 624 total, 623 pass / 1 skipped / 0 failed excluding `Persistence`; the DB-gated persistence tests fail only because `ConnectionStrings__DefaultConnection`/PostgreSQL is unavailable on this machine.
- PASS (office code-only) - Concept Registry v1: Release build `dotnet build AIStudio.slnx -c Release` 0 warnings/0 errors; `dotnet format AIStudio.slnx --verify-no-changes` clean; `git diff --check` clean. Full suite 624 total: 623 pass / 1 skipped (gated real audio E2E) / 0 failed excluding the `AIStudio.Tests.Persistence` namespace. Earlier Production Recipe v1 run: 572 total, 571 pass / 1 skipped / 0 failed excluding `Persistence`; the DB-gated persistence tests fail only because `ConnectionStrings__DefaultConnection`/PostgreSQL is unavailable on this machine.
- PASS (office code-only) - Production Recipe v1: Release build `dotnet build AIStudio.slnx -c Release` 0 warnings/0 errors; `dotnet format AIStudio.slnx --verify-no-changes` clean; `git diff --check` clean. Full suite 572 total: 571 pass / 1 skipped (gated real audio E2E) / 0 failed excluding the `AIStudio.Tests.Persistence` namespace. Earlier Capability Registry v1 run: 538 total, 524 pass / 1 skipped / 0 failed excluding `Persistence`; the DB-gated persistence tests fail only because `ConnectionStrings__DefaultConnection`/PostgreSQL is unavailable on this machine.
- PASS - Narrative Sync v1 focused tests: reviewed-script->scene mapping, TTS-safe normalization, per-scene narration reuse/regeneration, speech-driven scene timing, phrase subtitles constrained to scenes, narration-window choreography, FLUX no-text prompt, trusted overlay, explicit render durations, no GPU-gate/audio dependency.
- PASS - Narrative Sync v1 full suite: 501 total, 500 pass, 1 skipped (gated provider E2E), 0 failed; Release build 0 warnings; `dotnet format --verify-no-changes` and `git diff --check` clean.
- PASS - Real narrative-sync E2E (canonical project): GenerateAudio regenerated per-scene VoxCPM2 narration from the reviewed script (8 clips, 12.48-27.52s; scenes 0-2 reused after an interrupted partial run), assembled narration 190.51s, BGM regenerated, master 190.51s 48kHz stereo (peak -3.6 dBFS, integrated -17.5 LUFS). GenerateSceneVisuals regenerated all 8 scenes at the new speech-driven durations (ThreeD, Manim x3, AnimatedSvg x3, FLUX). `RenderVideo?variant=narrative-sync-v1` produced a 190.51s h264/aac 1280x720 MP4 with narrative subtitles burned in; FinalVideoQa pass. Canonical and `-motion-v1.mp4` artifacts untouched.

- PASS - Visual Direction v1 focused tests: canonical intent classification, deterministic router + fallbacks, choreography bounds/invalid-primitive rejection, frame reveal, subtitle safe area, per-scene fingerprint reuse/invalidation, forced regeneration, AnimatedSvg/FLUX renderer command shape, Blender seed clamp.
- PASS - Visual Direction v1 full suite: 488 total, 487 pass, 1 skipped (audio provider E2E gated), 0 failed.
- PASS - Real canonical multi-engine regeneration + render: `POST .../visual-jobs?force=true` completed in 260s generating 8/8 scenes (scene0 ThreeD/Blender, scene1 ManimAnimation/LocalAiFlow, scene2 AnimatedSvg, scene3/4 ManimAnimation/ProcessFlow, scene5/6 AnimatedSvg, scene7 AiImage/FLUX). GPU leases: blender x1, comfyui x1; ComfyUI executed the FLUX prompt (`~/Developer/Microservices/ai/ComfyUI/output/aistudio/scene_*.png`). A normal re-run without force reused all 8 (`generated=0, skipped=8`, all `reused`). `RenderVideo?variant=motion-v1` -> `renders/01b7f0efe7494e1dbbada93d016b2be2/1526be4b6d434f54999782cc368a1990-motion-v1.mp4` (h264/aac 1280x720 20.48s, decode clean, subtitles burned in); FinalVideoQa pass. Audio reused unchanged: `master.wav`/`narration.wav`/`music.wav` mtimes unchanged, no speech/music/ollama GPU leases.

- PASS - Production Orchestration v2 focused tests: GenerateAudio handler (all stages, reuse, only-failed-stage retry, input-change invalidation, missing stage file regeneration, disabled providers, stage error mapping, cancellation, no GPU-gate constructor) + workflow/contracts + updated RenderVideo master-consumption tests.
- PASS - Production Orchestration v2 full suite: 463 total, 462 pass, 1 skipped (real provider E2E gated by env vars), 0 failed, with `ConnectionStrings__DefaultConnection` set.
- PASS - Real E2E Video Production v2 (canonical project `01b7f0ef-e749-4e1d-bbad-a93d016b2be2`, storyboard `1526be4b-6d43-4f54-9997-82cc368a1990`): GenerateAudio -> RenderVideo -> FinalVideoQa all completed. Narration 48 kHz mono 20.48s; BGM 48 kHz stereo 23.0s; master 48 kHz stereo 20.48s, peak -3.5 dBFS, integrated -15.8 LUFS (no clipping); final MP4 h264/aac 1280x720 20.48s, `audioSource=mastered`, subtitles burned in, QA pass (video+audio present).
- PASS - Retry/idempotency proof (low cost, no GPU re-run): second GenerateAudio completed in 2s with `narrationReused=true`, `musicReused=true`, `masterReused=true`; stage WAV mtimes unchanged and content hashes identical.

- PASS - Audio Generation v1 focused tests: 45 pass (VoxCPM2 speech provider, ACE-Step music provider, ffprobe sample-rate/channel parsing), 0 failed.
- PASS - Audio Generation v1 full suite: 440 total, 439 pass, 1 skipped (real E2E gated by env vars), 0 failed.
- PASS - Real local E2E through the providers (VoxCPM2 `.venv` + local model; ACE-Step `.venv` + `acestep-v15-turbo`): narration WAV 48 kHz mono 6.72s; music WAV 48 kHz stereo 12.0s; then `IAudioMixer` produced `final_mix.wav` 48 kHz stereo 6.72s, peak -4.15 dBFS, integrated -17.0 LUFS (no clipping, bounded to narration length).

- PASS - Audio Mixing / Mastering v1: 24 focused tests pass (command plan, mixer, and a real-FFmpeg integration test); full suite 399/399 with `ConnectionStrings__DefaultConnection` set, 0 skipped.
- PASS - Audio mix real FFmpeg smoke: synthesized 6s mono narration + 3s stereo music -> 48 kHz stereo `pcm_s16le` WAV, 6.0s, peak -12.2 dBFS, integrated -15.9 LUFS (no clipping); ducking measured ~10 dB music reduction under speech and full recovery in a 2s silent gap.

- PASS (partial) - 365/375 tests pass, 0 skipped, without PostgreSQL. The 10 `Persistence` integration tests are DB-gated and were not run this session (PostgreSQL/Docker unavailable in this WSL distro); the last full-suite run with `ConnectionStrings__DefaultConnection` set was 338/338. New coverage: GPU resource gate acquisition/waiter/cancellation/exception/double-dispose/cross-workload exclusion, ComfyUI holding the lease until generation completes, Blender holding it only for the CUDA render and releasing before FFmpeg, and one shared singleton gate via DI. Existing provider error mapping is unchanged.
- PASS - Blender 3D engine end-to-end against local Blender 5.2.2 LTS (Cycles CUDA, RTX 4060 Ti 16 GB) through the real `BlenderThreeDRenderingProvider`: 768x432, 16 samples, 90 frames (3.0s @ 30 FPS), total 105s, valid H.264/yuv420p MP4 (`85788` bytes, `ffprobe` confirms 768x432 / 90 frames / 3.0s). The engine stays disabled by default (`Blender:Enabled=false`), so canonical SVG/Manim behavior is unchanged.
- PASS - `dotnet build AIStudio.slnx -c Release` 0 warnings/0 errors; `dotnet format AIStudio.slnx --verify-no-changes` clean; `git diff --check` clean.
- PASS - ComfyUI engine end-to-end against the live local server (`127.0.0.1:8188`, ComfyUI 0.36.0, RTX 4060 Ti 16 GB): both the approved template smoke test and the real `ComfyUiImageGenerationProvider` produced a valid 1280x720 PNG (`849165` bytes via the provider). The engine stays disabled by default (`ComfyUi:Enabled=false`), so canonical SVG/Manim behavior is unchanged.
- PASS - Video Quality v1.1 demo re-probed with `ffprobe`: 24s, 1280x720, h264, aac 48kHz stereo, styled subtitles burned in, full decode clean, SHA-256 recorded above. Mix loudness mean -18.1 / peak -13.8 dB, narration gain-staged and music ducked beneath it (no clipping).
- PASS - audio ducking calibrated on the real assets: with narration gain-staged and ratio 4, the ducked music peaks ~-33 dB, ~20 dB below the narration, and recovers between phrases.
- PASS - `librsvg` SVG->PNG rasterization verified in the installed FFmpeg 6.1.1 build (`svg_pipe` format, `librsvg` decoder).
- PASS - canonical Video #1 MP4 and `quality-v1.mp4` re-probed with `ffprobe`; SHA-256 values unchanged.
- PASS - `/health/ready` returns HTTP 200 (PostgreSQL Healthy) and the persistent job worker is active.

- PASS (HOME code-only) - Identity Asset Contract v1: focused Identity/Bibles/StoryContext/Architecture suite 256/256; full non-persistence suite 1019 total (1018 pass, 1 skipped gated real-audio E2E, 0 failed); Release build 0 warnings/0 errors. No database or Ollama/Qwen/ComfyUI/FLUX/Blender/VoxCPM/ACE-Step/Manim/FFmpeg media runtime was invoked. `dotnet format --verify-no-changes` and `git diff --check` are clean.
## Known issues and next task

- Extractable-module guardrails are documented and enforced only at the assembly level (layer directions). Finer-grained cross-module namespace rules (for example one in-assembly module referencing another module's Infrastructure namespace) are deliberately deferred until a second module actually exists; no architecture-testing package was added. Recommended next step while on the office machine: stop architecture expansion unless another clearly valuable code-only gap is identified; on the home machine, run real Qwen Creative Director validation. No Stock Intelligence, broker, DB split, or remote client is planned in the current Content Studio scope.
- Story Context Builder preserves optional `AssetReference.Version` when asset metadata is requested but intentionally performs no identity-asset resolution; production/materialization must call `IdentityAssetResolver` once and retain its concrete Approved pin.
- Character + World Bible now has the minimal Identity Asset Contract v1 and local byte store, but no Bible/identity-asset persistence, endpoint, catalog, provider consumption, or production wiring. The registry remains in-memory and physical paths remain Infrastructure-private.
- Story Director Foundation v1 is complete: no durable job or endpoint produces a `StoryPlan`, there is no Creative Director runtime integration, and the v1 director is deterministic structural scaffolding rather than story generation. A real Qwen Story Director and Script/Storyboard consumption of `StoryPlan` remain future.
- Visual Polish v1 (FIXED): the `process_flow` hourglass/bow-tie was the progress fill - `RoundedRectangle(width=0.05, height=0.34, corner_radius=0.17)` is degenerate (`corner_radius > width/2`, 6 self-intersections) and `scale_to_fit_width` then scaled its height out of frame. The fill is now a valid, constant-height, left-anchored capsule rebuilt each frame (`progress_fill`) and aligned to the window like the chips; `_rounded_rect` clamps corner radii so no shape can self-intersect. Verified by `Rendering/Manim/verify_geometry.py` and real targeted re-renders of scenes 3/4.

- `FinalVideoQa` `Job.Result` is persisted but not deserialized by `GET /api/jobs/{jobId}` (`GenerateIdeaWorkflow.FindJobAsync` omits `JobType.FinalVideoQa`); read it from PostgreSQL until a focused fix is authorised.
- Durable visual generation now exists (`GenerateSceneVisuals`), but the `SceneVisualBrief` direction is still the deterministic `SceneVisualPlanner` keyword heuristic. Letting Qwen emit structured visual direction through the same plan shape remains a future step; it is not required for Phase 3/4.
- Manim is optional and disabled by default. Durable `GenerateSceneVisuals` now derives the Manim clip duration from the same narration-aware `SceneTiming` allocation and falls back to the deterministic `AnimationDurationSeconds` (3.5s) floor only when narration is absent or unreadable.
- Durable subtitle re-timing applies only when the canonical SRT cue count equals the storyboard scene count (one cue per scene). Any other shape keeps the canonical timing as the deterministic fallback, so multi-cue SRTs are not re-timed yet.
- The `SceneVisualPlanner` concept vocabulary is generic but finite; a scene whose `visual` has no quoted labels or recognized concepts falls back to a heading-only card.
- `.demo/` is a local harness; generated runtime assets under `src/AIStudio.Api/assets/` stay gitignored and are preserved locally, not committed. AI image generation and 3D scene rendering now exist as optional engines (ComfyUI and Blender, both disabled by default). Heavy GPU workloads (ComfyUI, Blender CUDA, Ollama) are serialized by the process-local GPU gate; character animation, VRAM-aware scheduling, and cross-process coordination remain deferred.
- Production Orchestration v2 wires VoxCPM2/ACE-Step into the durable `GenerateAudio` job and `RenderVideo` consumes the mastered audio, but there is no job chaining: the sequence is enqueued stage-by-stage (storyboard -> visuals -> audio -> render -> QA). Stage reuse invalidates on content fingerprints only, not on provider configuration changes, unless the production stage version constant is bumped. `FinalVideoQa` result is still not deserialized by `GET /api/jobs/{id}` (pre-existing deferred fix); `GenerateAudio`/`RenderVideo` results are exposed. Stable Audio 3 remains an optional future provider.
- Parallel visual/audio generation is intentionally not implemented; v2 runs sequentially to keep retry safety simple.
- Visual routing is deterministic keyword intent, not a model-authored visual direction; the Creative Director model remains future. The narration-aware allocation can give a short closing scene a very small duration when animated floors exceed the narration length (a per-scene minimum for hero/closing scenes is a possible follow-up).
- Recovery note: an earlier interrupted session left `git stash` ('recovery: narrative sync interrupted'), the repo-owned `scripts/e2e-narrative-sync.sh`, and stale recovery notes in STATE/CHECKPOINT. The stash is left untouched; the stale notes are superseded by this update.
- Narrative Sync v1 E2E notes: the reviewed narration is ~3.8k characters (~3.2 min), so VoxCPM2 model load (~11 GB peak) required freeing memory during the run; quoted spans are normalized before synthesis. ACE-Step BGM is capped at 120s and looped by the mixer over the ~190s narration; raising `MaxMusicSeconds` would let one generation cover it. Subtitles are phrase-level (not word-level); no forced alignment.
- Next milestone: Identity Reference Consumption v1 for exactly ONE explicitly chosen engine. Resolve a floating Bible reference at materialization, pin the concrete Approved version into execution, and prove later approvals cannot change an existing materialized job/plan. Do not widen to multiple engines, a persistent/full registry, catalog/search, DB/API, or provider fields in Application.
