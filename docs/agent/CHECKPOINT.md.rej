--- docs/agent/CHECKPOINT.md
+++ docs/agent/CHECKPOINT.md
@@ -1,12 +1,15 @@
 # Agent Checkpoint
 
 Updated: 2026-09-15
 
 - Current phase: P1
-- Current milestone: Foundation — complete
-- Last completed task: .NET 10/React foundation with Clean Architecture references, PostgreSQL EF Core setup, DI, health checks, Problem Details, JSON logging, configuration validation, and EF tooling.
-- Build/test status: backend Release build PASS (0 warnings/errors); web WSL build PASS; automated tests NOT AVAILABLE (no test project).
-- Important files: `AIStudio.slnx`, `src/AIStudio.Api/Program.cs`, `src/AIStudio.Application/Abstractions/Persistence/IApplicationDbContext.cs`, `src/AIStudio.Infrastructure/DependencyInjection.cs`, `src/AIStudio.Infrastructure/Persistence/ApplicationDbContext.cs`, `README.md`.
-- Known issues: no PostgreSQL container/server is running, so readiness is correctly HTTP 503 and real connectivity is NOT AVAILABLE; persistent WSL SDK/Node installs still need operator setup.
+- Current milestone: P1.1 Local Development Runtime — complete
+- Last completed task: added a PostgreSQL-only Docker Compose runtime and verified the Foundation API against the real database.
+- PostgreSQL runtime: PASS — `postgres:16-alpine` is running healthy on `127.0.0.1:5432` with named volume `aistudio-postgres-data`.
+- Actual DB connectivity: PASS — PostgreSQL readiness, SQL query, EF context, and API-to-PostgreSQL connection verified.
+- Health checks: PASS — live/ready HTTP 200 with PostgreSQL; ready HTTP 503 and live HTTP 200 while PostgreSQL was stopped; recovery HTTP 200 verified.
+- Build/test status: backend restore and Release build PASS (0 warnings/errors); automated tests NOT AVAILABLE; web build NOT RUN because frontend was unaffected.
+- Important files: `compose.yaml`, `.env.example`, `README.md`, `src/AIStudio.Api/Program.cs`, `src/AIStudio.Infrastructure/DependencyInjection.cs`, `docs/context/STATE.md`.
+- Known issues: persistent WSL .NET 10 and Node >=20.19 installs are still required; uncached Docker Hub pulls currently fail client authentication. No migration exists by design.
 - Next recommended task: P1 Core Domain + Persistent Job Model, under a separate instruction.
 - Relevant areas next: `docs/development/ARCHITECTURE.md`, PRD §§13–15/24–26, Application/Domain/Infrastructure projects.
